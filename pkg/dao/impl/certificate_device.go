// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package impl

import (
	"context"
	"database/sql"
	"fmt"

	"dumpapp_server/pkg/common/clients"
	"dumpapp_server/pkg/common/errors"
	"dumpapp_server/pkg/dao/models"
	mysqlDriver "github.com/go-sql-driver/mysql"
	pkgErr "github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
)

type CertificateDeviceDAO struct {
	mysqlPool *sql.DB
}

var DefaultCertificateDeviceDAO *CertificateDeviceDAO

func init() {
	DefaultCertificateDeviceDAO = NewCertificateDeviceDAO()
}

func NewCertificateDeviceDAO() *CertificateDeviceDAO {
	d := &CertificateDeviceDAO{
		mysqlPool: clients.MySQLConnectionsPool,
	}
	return d
}

func (d *CertificateDeviceDAO) Insert(ctx context.Context, data *models.CertificateDevice) error {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := data.Insert(ctx, exec, boil.Infer())
	if err != nil {
		if mysqlError, ok := pkgErr.Cause(err).(*mysqlDriver.MySQLError); !(ok && mysqlError.Number == 1062) {
			return pkgErr.WithStack(err)
		}
	}
	return nil
}

func (d *CertificateDeviceDAO) Update(ctx context.Context, data *models.CertificateDevice) error {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	_, err := data.Update(ctx, exec, boil.Infer())
	return pkgErr.WithStack(err)
}

func (d *CertificateDeviceDAO) Delete(ctx context.Context, id int64) error {
	qs := []qm.QueryMod{
		models.CertificateDeviceWhere.ID.EQ(id),
	}

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	_, err := models.CertificateDevices(qs...).DeleteAll(ctx, exec)
	if err != nil {
		return pkgErr.WithStack(err)
	}
	return nil
}

func (d *CertificateDeviceDAO) Get(ctx context.Context, id int64) (*models.CertificateDevice, error) {
	result, err := d.BatchGet(ctx, []int64{id})
	if err != nil {
		return nil, err
	}
	if v, ok := result[id]; !ok {
		return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=certificate_device, id=%d", id)
	} else {
		return v, nil
	}
}

// BatchGet retrieves multiple records by primary key from db.
func (d *CertificateDeviceDAO) BatchGet(ctx context.Context, ids []int64) (map[int64]*models.CertificateDevice, error) {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	datas, err := models.CertificateDevices(models.CertificateDeviceWhere.ID.IN(ids)).All(ctx, exec)
	if err != nil {
		return nil, pkgErr.WithStack(err)
	}

	result := make(map[int64]*models.CertificateDevice)
	for _, c := range datas {
		result[c.ID] = c
	}

	return result, nil
}

// 后台和脚本使用：倒序列出所有
func (d *CertificateDeviceDAO) ListIDs(ctx context.Context, offset, limit int, filters []qm.QueryMod, orderBys []string) ([]int64, error) {
	if offset < 0 || limit <= 0 || limit > 10000 {
		return nil, pkgErr.Errorf("invalid offset or limit")
	}
	qs := []qm.QueryMod{qm.Select(models.CertificateDeviceColumns.ID)}
	qs = append(qs, filters...)

	if len(orderBys) > 0 {
		orderBys = append(orderBys, "id desc")
		for _, orderBy := range orderBys {
			qs = append(qs, qm.OrderBy(orderBy))
		}
	} else {
		qs = append(qs, qm.OrderBy("created_at DESC, id DESC"))
	}

	if offset >= 0 && limit >= 0 {
		qs = append(qs, qm.Offset(offset), qm.Limit(limit))
	}

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	datas, err := models.CertificateDevices(qs...).All(ctx, exec)
	if err != nil {
		return nil, pkgErr.Wrap(err, fmt.Sprintf("table=certificate_device offset=%d limit=%d filters=%v", offset, limit, filters))
	}

	result := make([]int64, 0)
	for _, c := range datas {
		result = append(result, c.ID)
	}
	return result, nil
}

func (d *CertificateDeviceDAO) Count(ctx context.Context, filters []qm.QueryMod) (int64, error) {
	qs := []qm.QueryMod{qm.Select(models.CertificateDeviceColumns.ID)}
	qs = append(qs, filters...)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	return models.CertificateDevices(qs...).Count(ctx, exec)
}

// GetByDeviceIDCertificateID retrieves a single record by uniq key deviceID, certificateID from db.
func (d *CertificateDeviceDAO) GetByDeviceIDCertificateID(ctx context.Context, deviceID int64, certificateID int64) (*models.CertificateDevice, error) {
	certificateDeviceObj := &models.CertificateDevice{}

	sel := "*"
	query := fmt.Sprintf(
		"select %s from `certificate_device` where `device_id`=? AND `certificate_id`=?", sel,
	)

	q := queries.Raw(query, deviceID, certificateID)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := q.Bind(ctx, exec, certificateDeviceObj)
	if err != nil {
		if pkgErr.Cause(err) == sql.ErrNoRows {
			return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=certificate_device, query=%s, args=deviceID:%v certificateID :%v", query, deviceID, certificateID)
		}
		return nil, pkgErr.Wrap(err, "dao: unable to select from certificate_device")
	}

	return certificateDeviceObj, nil
}

// GetCertificateDeviceSliceByDeviceID retrieves a slice of records by first field of uniq key [deviceID] with an executor.
func (d *CertificateDeviceDAO) GetCertificateDeviceSliceByDeviceID(ctx context.Context, deviceID int64) ([]*models.CertificateDevice, error) {
	var o []*models.CertificateDevice

	query := "select `certificate_device`.* from `certificate_device` where `device_id`=?"

	q := queries.Raw(query, deviceID)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		if pkgErr.Cause(err) == sql.ErrNoRows {
			return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=certificate_device, query=%s, args=deviceID :%v", query, deviceID)
		}
		return nil, pkgErr.Wrap(err, "dao: unable to select from certificate_device")
	}
	return o, nil
}
