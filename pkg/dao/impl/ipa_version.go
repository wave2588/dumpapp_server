// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package impl

import (
	"context"
	"database/sql"
	"fmt"

	"dumpapp_server/pkg/common/clients"
	"dumpapp_server/pkg/common/errors"
	"dumpapp_server/pkg/dao/models"
	mysqlDriver "github.com/go-sql-driver/mysql"
	pkgErr "github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
)

type IpaVersionDAO struct {
	mysqlPool *sql.DB
}

var DefaultIpaVersionDAO *IpaVersionDAO

func init() {
	DefaultIpaVersionDAO = NewIpaVersionDAO()
}

func NewIpaVersionDAO() *IpaVersionDAO {
	d := &IpaVersionDAO{
		mysqlPool: clients.MySQLConnectionsPool,
	}
	return d
}

func (d *IpaVersionDAO) Insert(ctx context.Context, data *models.IpaVersion) error {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := data.Insert(ctx, exec, boil.Infer())
	if err != nil {
		if mysqlError, ok := pkgErr.Cause(err).(*mysqlDriver.MySQLError); !(ok && mysqlError.Number == 1062) {
			return pkgErr.WithStack(err)
		}
	}
	return nil
}

func (d *IpaVersionDAO) Update(ctx context.Context, data *models.IpaVersion) error {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	_, err := data.Update(ctx, exec, boil.Infer())
	return pkgErr.WithStack(err)
}

func (d *IpaVersionDAO) Delete(ctx context.Context, id int64) error {
	qs := []qm.QueryMod{
		models.IpaVersionWhere.ID.EQ(id),
	}

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	_, err := models.IpaVersions(qs...).DeleteAll(ctx, exec)
	if err != nil {
		return pkgErr.WithStack(err)
	}
	return nil
}

func (d *IpaVersionDAO) Get(ctx context.Context, id int64) (*models.IpaVersion, error) {
	result, err := d.BatchGet(ctx, []int64{id})
	if err != nil {
		return nil, err
	}
	if v, ok := result[id]; !ok {
		return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=ipa_version, id=%d", id)
	} else {
		return v, nil
	}
}

// BatchGet retrieves multiple records by primary key from db.
func (d *IpaVersionDAO) BatchGet(ctx context.Context, ids []int64) (map[int64]*models.IpaVersion, error) {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	datas, err := models.IpaVersions(models.IpaVersionWhere.ID.IN(ids)).All(ctx, exec)
	if err != nil {
		return nil, pkgErr.WithStack(err)
	}

	result := make(map[int64]*models.IpaVersion)
	for _, c := range datas {
		result[c.ID] = c
	}

	return result, nil
}

// 后台和脚本使用：倒序列出所有
func (d *IpaVersionDAO) ListIDs(ctx context.Context, offset, limit int, filters []qm.QueryMod, orderBys []string) ([]int64, error) {
	if offset < 0 || limit <= 0 || limit > 10000 {
		return nil, pkgErr.Errorf("invalid offset or limit")
	}
	qs := []qm.QueryMod{qm.Select(models.IpaVersionColumns.ID)}
	qs = append(qs, filters...)

	if len(orderBys) > 0 {
		orderBys = append(orderBys, "id desc")
		for _, orderBy := range orderBys {
			qs = append(qs, qm.OrderBy(orderBy))
		}
	} else {
		qs = append(qs, qm.OrderBy("created_at DESC, id DESC"))
	}

	if offset >= 0 && limit >= 0 {
		qs = append(qs, qm.Offset(offset), qm.Limit(limit))
	}

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	datas, err := models.IpaVersions(qs...).All(ctx, exec)
	if err != nil {
		return nil, pkgErr.Wrap(err, fmt.Sprintf("table=ipa_version offset=%d limit=%d filters=%v", offset, limit, filters))
	}

	result := make([]int64, 0)
	for _, c := range datas {
		result = append(result, c.ID)
	}
	return result, nil
}

func (d *IpaVersionDAO) Count(ctx context.Context, filters []qm.QueryMod) (int64, error) {
	qs := []qm.QueryMod{qm.Select(models.IpaVersionColumns.ID)}
	qs = append(qs, filters...)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	return models.IpaVersions(qs...).Count(ctx, exec)
}

// GetByIpaIDVersion retrieves a single record by uniq key ipaID, version from db.
func (d *IpaVersionDAO) GetByIpaIDVersion(ctx context.Context, ipaID int64, version string) (*models.IpaVersion, error) {
	ipaVersionObj := &models.IpaVersion{}

	sel := "*"
	query := fmt.Sprintf(
		"select %s from `ipa_version` where `ipa_id`=? AND `version`=?", sel,
	)

	q := queries.Raw(query, ipaID, version)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := q.Bind(ctx, exec, ipaVersionObj)
	if err != nil {
		if pkgErr.Cause(err) == sql.ErrNoRows {
			return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=ipa_version, query=%s, args=ipaID:%v version :%v", query, ipaID, version)
		}
		return nil, pkgErr.Wrap(err, "dao: unable to select from ipa_version")
	}

	return ipaVersionObj, nil
}

// GetIpaVersionSliceByIpaID retrieves a slice of records by first field of uniq key [ipaID] with an executor.
func (d *IpaVersionDAO) GetIpaVersionSliceByIpaID(ctx context.Context, ipaID int64) ([]*models.IpaVersion, error) {
	var o []*models.IpaVersion

	query := "select `ipa_version`.* from `ipa_version` where `ipa_id`=?"

	q := queries.Raw(query, ipaID)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		if pkgErr.Cause(err) == sql.ErrNoRows {
			return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=ipa_version, query=%s, args=ipaID :%v", query, ipaID)
		}
		return nil, pkgErr.Wrap(err, "dao: unable to select from ipa_version")
	}
	return o, nil
}

// GetByTokenPath retrieves a single record by uniq key tokenPath from db.
func (d *IpaVersionDAO) GetByTokenPath(ctx context.Context, tokenPath string) (*models.IpaVersion, error) {
	ipaVersionObj := &models.IpaVersion{}

	sel := "*"
	query := fmt.Sprintf(
		"select %s from `ipa_version` where `token_path`=?", sel,
	)

	q := queries.Raw(query, tokenPath)

	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}

	err := q.Bind(ctx, exec, ipaVersionObj)
	if err != nil {
		if pkgErr.Cause(err) == sql.ErrNoRows {
			return nil, pkgErr.Wrapf(errors.ErrNotFound, "table=ipa_version, query=%s, args=tokenPath :%v", query, tokenPath)
		}
		return nil, pkgErr.Wrap(err, "dao: unable to select from ipa_version")
	}

	return ipaVersionObj, nil
}

// BatchGetByTokenPath retrieves multiple records by uniq key tokenPath from db.
func (d *IpaVersionDAO) BatchGetByTokenPath(ctx context.Context, tokenPaths []string) (map[string]*models.IpaVersion, error) {
	var exec boil.ContextExecutor
	txn := ctx.Value("txn")
	if txn == nil {
		exec = d.mysqlPool
	} else {
		exec = txn.(*sql.Tx)
	}
	datas, err := models.IpaVersions(models.IpaVersionWhere.TokenPath.IN(tokenPaths)).All(ctx, exec)
	if err != nil {
		return nil, pkgErr.WithStack(err)
	}

	result := make(map[string]*models.IpaVersion)
	for _, c := range datas {
		result[c.TokenPath] = c
	}

	return result, nil
}
