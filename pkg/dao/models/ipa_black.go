// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/datatype"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// IpaBlack is an object representing the database table.
type IpaBlack struct {
	ID        int64                   `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	IpaID     int64                   `boil:"ipa_id" json:"ipa_id" toml:"ipa_id" yaml:"ipa_id"`
	BizExt    datatype.IpaBlackBizExt `boil:"biz_ext" json:"biz_ext" toml:"biz_ext" yaml:"biz_ext"`
	CreatedAt time.Time               `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time               `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *ipaBlackR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ipaBlackL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IpaBlackColumns = struct {
	ID        string
	IpaID     string
	BizExt    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	IpaID:     "ipa_id",
	BizExt:    "biz_ext",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

var IpaBlackTableColumns = struct {
	ID        string
	IpaID     string
	BizExt    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "ipa_black.id",
	IpaID:     "ipa_black.ipa_id",
	BizExt:    "ipa_black.biz_ext",
	CreatedAt: "ipa_black.created_at",
	UpdatedAt: "ipa_black.updated_at",
}

// Generated where

type whereHelperdatatype_IpaBlackBizExt struct{ field string }

func (w whereHelperdatatype_IpaBlackBizExt) EQ(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperdatatype_IpaBlackBizExt) NEQ(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperdatatype_IpaBlackBizExt) LT(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperdatatype_IpaBlackBizExt) LTE(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperdatatype_IpaBlackBizExt) GT(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperdatatype_IpaBlackBizExt) GTE(x datatype.IpaBlackBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var IpaBlackWhere = struct {
	ID        whereHelperint64
	IpaID     whereHelperint64
	BizExt    whereHelperdatatype_IpaBlackBizExt
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`ipa_black`.`id`"},
	IpaID:     whereHelperint64{field: "`ipa_black`.`ipa_id`"},
	BizExt:    whereHelperdatatype_IpaBlackBizExt{field: "`ipa_black`.`biz_ext`"},
	CreatedAt: whereHelpertime_Time{field: "`ipa_black`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`ipa_black`.`updated_at`"},
}

// IpaBlackRels is where relationship names are stored.
var IpaBlackRels = struct {
}{}

// ipaBlackR is where relationships are stored.
type ipaBlackR struct {
}

// NewStruct creates a new relationship struct
func (*ipaBlackR) NewStruct() *ipaBlackR {
	return &ipaBlackR{}
}

// ipaBlackL is where Load methods for each relationship are stored.
type ipaBlackL struct{}

var (
	ipaBlackAllColumns            = []string{"id", "ipa_id", "biz_ext", "created_at", "updated_at"}
	ipaBlackColumnsWithoutDefault = []string{"ipa_id", "biz_ext"}
	ipaBlackColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	ipaBlackPrimaryKeyColumns     = []string{"id"}
	ipaBlackGeneratedColumns      = []string{}
)

type (
	// IpaBlackSlice is an alias for a slice of pointers to IpaBlack.
	// This should almost always be used instead of []IpaBlack.
	IpaBlackSlice []*IpaBlack
	// IpaBlackHook is the signature for custom IpaBlack hook methods
	IpaBlackHook func(context.Context, boil.ContextExecutor, *IpaBlack) error

	ipaBlackQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ipaBlackType                 = reflect.TypeOf(&IpaBlack{})
	ipaBlackMapping              = queries.MakeStructMapping(ipaBlackType)
	ipaBlackPrimaryKeyMapping, _ = queries.BindMapping(ipaBlackType, ipaBlackMapping, ipaBlackPrimaryKeyColumns)
	ipaBlackInsertCacheMut       sync.RWMutex
	ipaBlackInsertCache          = make(map[string]insertCache)
	ipaBlackUpdateCacheMut       sync.RWMutex
	ipaBlackUpdateCache          = make(map[string]updateCache)
	ipaBlackUpsertCacheMut       sync.RWMutex
	ipaBlackUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ipaBlackAfterSelectHooks []IpaBlackHook

var ipaBlackBeforeInsertHooks []IpaBlackHook
var ipaBlackAfterInsertHooks []IpaBlackHook

var ipaBlackBeforeUpdateHooks []IpaBlackHook
var ipaBlackAfterUpdateHooks []IpaBlackHook

var ipaBlackBeforeDeleteHooks []IpaBlackHook
var ipaBlackAfterDeleteHooks []IpaBlackHook

var ipaBlackBeforeUpsertHooks []IpaBlackHook
var ipaBlackAfterUpsertHooks []IpaBlackHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IpaBlack) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IpaBlack) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IpaBlack) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IpaBlack) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IpaBlack) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IpaBlack) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IpaBlack) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IpaBlack) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IpaBlack) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaBlackAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIpaBlackHook registers your hook function for all future operations.
func AddIpaBlackHook(hookPoint boil.HookPoint, ipaBlackHook IpaBlackHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ipaBlackAfterSelectHooks = append(ipaBlackAfterSelectHooks, ipaBlackHook)
	case boil.BeforeInsertHook:
		ipaBlackBeforeInsertHooks = append(ipaBlackBeforeInsertHooks, ipaBlackHook)
	case boil.AfterInsertHook:
		ipaBlackAfterInsertHooks = append(ipaBlackAfterInsertHooks, ipaBlackHook)
	case boil.BeforeUpdateHook:
		ipaBlackBeforeUpdateHooks = append(ipaBlackBeforeUpdateHooks, ipaBlackHook)
	case boil.AfterUpdateHook:
		ipaBlackAfterUpdateHooks = append(ipaBlackAfterUpdateHooks, ipaBlackHook)
	case boil.BeforeDeleteHook:
		ipaBlackBeforeDeleteHooks = append(ipaBlackBeforeDeleteHooks, ipaBlackHook)
	case boil.AfterDeleteHook:
		ipaBlackAfterDeleteHooks = append(ipaBlackAfterDeleteHooks, ipaBlackHook)
	case boil.BeforeUpsertHook:
		ipaBlackBeforeUpsertHooks = append(ipaBlackBeforeUpsertHooks, ipaBlackHook)
	case boil.AfterUpsertHook:
		ipaBlackAfterUpsertHooks = append(ipaBlackAfterUpsertHooks, ipaBlackHook)
	}
}

// One returns a single ipaBlack record from the query.
func (q ipaBlackQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IpaBlack, error) {
	o := &IpaBlack{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ipa_black")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all IpaBlack records from the query.
func (q ipaBlackQuery) All(ctx context.Context, exec boil.ContextExecutor) (IpaBlackSlice, error) {
	var o []*IpaBlack

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to IpaBlack slice")
	}

	if len(ipaBlackAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all IpaBlack records in the query.
func (q ipaBlackQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ipa_black rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ipaBlackQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ipa_black exists")
	}

	return count > 0, nil
}

// IpaBlacks retrieves all the records using an executor.
func IpaBlacks(mods ...qm.QueryMod) ipaBlackQuery {
	mods = append(mods, qm.From("`ipa_black`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ipa_black`.*"})
	}

	return ipaBlackQuery{q}
}

// FindIpaBlack retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIpaBlack(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*IpaBlack, error) {
	ipaBlackObj := &IpaBlack{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ipa_black` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, ipaBlackObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ipa_black")
	}

	if err = ipaBlackObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ipaBlackObj, err
	}

	return ipaBlackObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IpaBlack) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ipa_black provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ipaBlackColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ipaBlackInsertCacheMut.RLock()
	cache, cached := ipaBlackInsertCache[key]
	ipaBlackInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ipaBlackAllColumns,
			ipaBlackColumnsWithDefault,
			ipaBlackColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ipa_black` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ipa_black` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ipa_black` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ipaBlackPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ipa_black")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == ipaBlackMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ipa_black")
	}

CacheNoHooks:
	if !cached {
		ipaBlackInsertCacheMut.Lock()
		ipaBlackInsertCache[key] = cache
		ipaBlackInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the IpaBlack.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IpaBlack) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ipaBlackUpdateCacheMut.RLock()
	cache, cached := ipaBlackUpdateCache[key]
	ipaBlackUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ipaBlackAllColumns,
			ipaBlackPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ipa_black, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ipa_black` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ipaBlackPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, append(wl, ipaBlackPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ipa_black row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ipa_black")
	}

	if !cached {
		ipaBlackUpdateCacheMut.Lock()
		ipaBlackUpdateCache[key] = cache
		ipaBlackUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ipaBlackQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ipa_black")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ipa_black")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IpaBlackSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaBlackPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ipa_black` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaBlackPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ipaBlack slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ipaBlack")
	}
	return rowsAff, nil
}

var mySQLIpaBlackUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IpaBlack) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ipa_black provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ipaBlackColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLIpaBlackUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ipaBlackUpsertCacheMut.RLock()
	cache, cached := ipaBlackUpsertCache[key]
	ipaBlackUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			ipaBlackAllColumns,
			ipaBlackColumnsWithDefault,
			ipaBlackColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ipaBlackAllColumns,
			ipaBlackPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ipa_black, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`ipa_black`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ipa_black` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ipa_black")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == ipaBlackMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ipaBlackType, ipaBlackMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ipa_black")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ipa_black")
	}

CacheNoHooks:
	if !cached {
		ipaBlackUpsertCacheMut.Lock()
		ipaBlackUpsertCache[key] = cache
		ipaBlackUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single IpaBlack record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IpaBlack) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no IpaBlack provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ipaBlackPrimaryKeyMapping)
	sql := "DELETE FROM `ipa_black` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ipa_black")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ipa_black")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ipaBlackQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ipaBlackQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ipa_black")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ipa_black")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IpaBlackSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ipaBlackBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaBlackPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ipa_black` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaBlackPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ipaBlack slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ipa_black")
	}

	if len(ipaBlackAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IpaBlack) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIpaBlack(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IpaBlackSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IpaBlackSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaBlackPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ipa_black`.* FROM `ipa_black` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaBlackPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in IpaBlackSlice")
	}

	*o = slice

	return nil
}

// IpaBlackExists checks if the IpaBlack row exists.
func IpaBlackExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ipa_black` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ipa_black exists")
	}

	return exists, nil
}

// Exists checks if the IpaBlack row exists.
func (o *IpaBlack) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return IpaBlackExists(ctx, exec, o.ID)
}
