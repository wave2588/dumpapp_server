// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberSignIpaDownloadCount is an object representing the database table.
type MemberSignIpaDownloadCount struct {
	ID        int64                                 `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64                                 `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Status    enum.MemberSignIpaDownloadCountStatus `boil:"status" json:"status" toml:"status" yaml:"status"`
	CreatedAt time.Time                             `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time                             `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberSignIpaDownloadCountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberSignIpaDownloadCountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberSignIpaDownloadCountColumns = struct {
	ID        string
	MemberID  string
	Status    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Status:    "status",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelperenum_MemberSignIpaDownloadCountStatus struct{ field string }

func (w whereHelperenum_MemberSignIpaDownloadCountStatus) EQ(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountStatus) NEQ(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountStatus) LT(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountStatus) LTE(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountStatus) GT(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountStatus) GTE(x enum.MemberSignIpaDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberSignIpaDownloadCountWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Status    whereHelperenum_MemberSignIpaDownloadCountStatus
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_sign_ipa_download_count`.`id`"},
	MemberID:  whereHelperint64{field: "`member_sign_ipa_download_count`.`member_id`"},
	Status:    whereHelperenum_MemberSignIpaDownloadCountStatus{field: "`member_sign_ipa_download_count`.`status`"},
	CreatedAt: whereHelpertime_Time{field: "`member_sign_ipa_download_count`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_sign_ipa_download_count`.`updated_at`"},
}

// MemberSignIpaDownloadCountRels is where relationship names are stored.
var MemberSignIpaDownloadCountRels = struct {
}{}

// memberSignIpaDownloadCountR is where relationships are stored.
type memberSignIpaDownloadCountR struct {
}

// NewStruct creates a new relationship struct
func (*memberSignIpaDownloadCountR) NewStruct() *memberSignIpaDownloadCountR {
	return &memberSignIpaDownloadCountR{}
}

// memberSignIpaDownloadCountL is where Load methods for each relationship are stored.
type memberSignIpaDownloadCountL struct{}

var (
	memberSignIpaDownloadCountAllColumns            = []string{"id", "member_id", "status", "created_at", "updated_at"}
	memberSignIpaDownloadCountColumnsWithoutDefault = []string{"member_id", "status"}
	memberSignIpaDownloadCountColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	memberSignIpaDownloadCountPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberSignIpaDownloadCountSlice is an alias for a slice of pointers to MemberSignIpaDownloadCount.
	// This should almost always be used instead of []MemberSignIpaDownloadCount.
	MemberSignIpaDownloadCountSlice []*MemberSignIpaDownloadCount
	// MemberSignIpaDownloadCountHook is the signature for custom MemberSignIpaDownloadCount hook methods
	MemberSignIpaDownloadCountHook func(context.Context, boil.ContextExecutor, *MemberSignIpaDownloadCount) error

	memberSignIpaDownloadCountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberSignIpaDownloadCountType                 = reflect.TypeOf(&MemberSignIpaDownloadCount{})
	memberSignIpaDownloadCountMapping              = queries.MakeStructMapping(memberSignIpaDownloadCountType)
	memberSignIpaDownloadCountPrimaryKeyMapping, _ = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, memberSignIpaDownloadCountPrimaryKeyColumns)
	memberSignIpaDownloadCountInsertCacheMut       sync.RWMutex
	memberSignIpaDownloadCountInsertCache          = make(map[string]insertCache)
	memberSignIpaDownloadCountUpdateCacheMut       sync.RWMutex
	memberSignIpaDownloadCountUpdateCache          = make(map[string]updateCache)
	memberSignIpaDownloadCountUpsertCacheMut       sync.RWMutex
	memberSignIpaDownloadCountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberSignIpaDownloadCountBeforeInsertHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountBeforeUpdateHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountBeforeDeleteHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountBeforeUpsertHooks []MemberSignIpaDownloadCountHook

var memberSignIpaDownloadCountAfterInsertHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountAfterSelectHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountAfterUpdateHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountAfterDeleteHooks []MemberSignIpaDownloadCountHook
var memberSignIpaDownloadCountAfterUpsertHooks []MemberSignIpaDownloadCountHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberSignIpaDownloadCount) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberSignIpaDownloadCount) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberSignIpaDownloadCount) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberSignIpaDownloadCount) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberSignIpaDownloadCount) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberSignIpaDownloadCount) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberSignIpaDownloadCount) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberSignIpaDownloadCount) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberSignIpaDownloadCount) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberSignIpaDownloadCountHook registers your hook function for all future operations.
func AddMemberSignIpaDownloadCountHook(hookPoint boil.HookPoint, memberSignIpaDownloadCountHook MemberSignIpaDownloadCountHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberSignIpaDownloadCountBeforeInsertHooks = append(memberSignIpaDownloadCountBeforeInsertHooks, memberSignIpaDownloadCountHook)
	case boil.BeforeUpdateHook:
		memberSignIpaDownloadCountBeforeUpdateHooks = append(memberSignIpaDownloadCountBeforeUpdateHooks, memberSignIpaDownloadCountHook)
	case boil.BeforeDeleteHook:
		memberSignIpaDownloadCountBeforeDeleteHooks = append(memberSignIpaDownloadCountBeforeDeleteHooks, memberSignIpaDownloadCountHook)
	case boil.BeforeUpsertHook:
		memberSignIpaDownloadCountBeforeUpsertHooks = append(memberSignIpaDownloadCountBeforeUpsertHooks, memberSignIpaDownloadCountHook)
	case boil.AfterInsertHook:
		memberSignIpaDownloadCountAfterInsertHooks = append(memberSignIpaDownloadCountAfterInsertHooks, memberSignIpaDownloadCountHook)
	case boil.AfterSelectHook:
		memberSignIpaDownloadCountAfterSelectHooks = append(memberSignIpaDownloadCountAfterSelectHooks, memberSignIpaDownloadCountHook)
	case boil.AfterUpdateHook:
		memberSignIpaDownloadCountAfterUpdateHooks = append(memberSignIpaDownloadCountAfterUpdateHooks, memberSignIpaDownloadCountHook)
	case boil.AfterDeleteHook:
		memberSignIpaDownloadCountAfterDeleteHooks = append(memberSignIpaDownloadCountAfterDeleteHooks, memberSignIpaDownloadCountHook)
	case boil.AfterUpsertHook:
		memberSignIpaDownloadCountAfterUpsertHooks = append(memberSignIpaDownloadCountAfterUpsertHooks, memberSignIpaDownloadCountHook)
	}
}

// One returns a single memberSignIpaDownloadCount record from the query.
func (q memberSignIpaDownloadCountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberSignIpaDownloadCount, error) {
	o := &MemberSignIpaDownloadCount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_sign_ipa_download_count")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberSignIpaDownloadCount records from the query.
func (q memberSignIpaDownloadCountQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberSignIpaDownloadCountSlice, error) {
	var o []*MemberSignIpaDownloadCount

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberSignIpaDownloadCount slice")
	}

	if len(memberSignIpaDownloadCountAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberSignIpaDownloadCount records in the query.
func (q memberSignIpaDownloadCountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_sign_ipa_download_count rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberSignIpaDownloadCountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_sign_ipa_download_count exists")
	}

	return count > 0, nil
}

// MemberSignIpaDownloadCounts retrieves all the records using an executor.
func MemberSignIpaDownloadCounts(mods ...qm.QueryMod) memberSignIpaDownloadCountQuery {
	mods = append(mods, qm.From("`member_sign_ipa_download_count`"))
	return memberSignIpaDownloadCountQuery{NewQuery(mods...)}
}

// FindMemberSignIpaDownloadCount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberSignIpaDownloadCount(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberSignIpaDownloadCount, error) {
	memberSignIpaDownloadCountObj := &MemberSignIpaDownloadCount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_sign_ipa_download_count` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberSignIpaDownloadCountObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_sign_ipa_download_count")
	}

	if err = memberSignIpaDownloadCountObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberSignIpaDownloadCountObj, err
	}

	return memberSignIpaDownloadCountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberSignIpaDownloadCount) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_sign_ipa_download_count provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberSignIpaDownloadCountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberSignIpaDownloadCountInsertCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountInsertCache[key]
	memberSignIpaDownloadCountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberSignIpaDownloadCountAllColumns,
			memberSignIpaDownloadCountColumnsWithDefault,
			memberSignIpaDownloadCountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_sign_ipa_download_count` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_sign_ipa_download_count` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_sign_ipa_download_count` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberSignIpaDownloadCountPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_sign_ipa_download_count")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberSignIpaDownloadCountMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_sign_ipa_download_count")
	}

CacheNoHooks:
	if !cached {
		memberSignIpaDownloadCountInsertCacheMut.Lock()
		memberSignIpaDownloadCountInsertCache[key] = cache
		memberSignIpaDownloadCountInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberSignIpaDownloadCount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberSignIpaDownloadCount) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberSignIpaDownloadCountUpdateCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountUpdateCache[key]
	memberSignIpaDownloadCountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberSignIpaDownloadCountAllColumns,
			memberSignIpaDownloadCountPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_sign_ipa_download_count, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_sign_ipa_download_count` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberSignIpaDownloadCountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, append(wl, memberSignIpaDownloadCountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_sign_ipa_download_count row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_sign_ipa_download_count")
	}

	if !cached {
		memberSignIpaDownloadCountUpdateCacheMut.Lock()
		memberSignIpaDownloadCountUpdateCache[key] = cache
		memberSignIpaDownloadCountUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberSignIpaDownloadCountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_sign_ipa_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_sign_ipa_download_count")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberSignIpaDownloadCountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_sign_ipa_download_count` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberSignIpaDownloadCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberSignIpaDownloadCount")
	}
	return rowsAff, nil
}

var mySQLMemberSignIpaDownloadCountUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberSignIpaDownloadCount) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_sign_ipa_download_count provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberSignIpaDownloadCountColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberSignIpaDownloadCountUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberSignIpaDownloadCountUpsertCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountUpsertCache[key]
	memberSignIpaDownloadCountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberSignIpaDownloadCountAllColumns,
			memberSignIpaDownloadCountColumnsWithDefault,
			memberSignIpaDownloadCountColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberSignIpaDownloadCountAllColumns,
			memberSignIpaDownloadCountPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_sign_ipa_download_count, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_sign_ipa_download_count`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_sign_ipa_download_count` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_sign_ipa_download_count")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberSignIpaDownloadCountMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberSignIpaDownloadCountType, memberSignIpaDownloadCountMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_sign_ipa_download_count")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_sign_ipa_download_count")
	}

CacheNoHooks:
	if !cached {
		memberSignIpaDownloadCountUpsertCacheMut.Lock()
		memberSignIpaDownloadCountUpsertCache[key] = cache
		memberSignIpaDownloadCountUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberSignIpaDownloadCount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberSignIpaDownloadCount) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberSignIpaDownloadCount provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberSignIpaDownloadCountPrimaryKeyMapping)
	sql := "DELETE FROM `member_sign_ipa_download_count` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_sign_ipa_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_sign_ipa_download_count")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberSignIpaDownloadCountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberSignIpaDownloadCountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_sign_ipa_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_sign_ipa_download_count")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberSignIpaDownloadCountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberSignIpaDownloadCountBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_sign_ipa_download_count` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberSignIpaDownloadCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_sign_ipa_download_count")
	}

	if len(memberSignIpaDownloadCountAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberSignIpaDownloadCount) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberSignIpaDownloadCount(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberSignIpaDownloadCountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberSignIpaDownloadCountSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_sign_ipa_download_count`.* FROM `member_sign_ipa_download_count` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberSignIpaDownloadCountSlice")
	}

	*o = slice

	return nil
}

// MemberSignIpaDownloadCountExists checks if the MemberSignIpaDownloadCount row exists.
func MemberSignIpaDownloadCountExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_sign_ipa_download_count` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_sign_ipa_download_count exists")
	}

	return exists, nil
}
