// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberDownloadOrder is an object representing the database table.
type MemberDownloadOrder struct {
	ID        int64                          `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64                          `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Status    enum.MemberDownloadOrderStatus `boil:"status" json:"status" toml:"status" yaml:"status"`
	Number    int64                          `boil:"number" json:"number" toml:"number" yaml:"number"`
	CreatedAt time.Time                      `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time                      `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberDownloadOrderR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberDownloadOrderL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberDownloadOrderColumns = struct {
	ID        string
	MemberID  string
	Status    string
	Number    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Status:    "status",
	Number:    "number",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelperenum_MemberDownloadOrderStatus struct{ field string }

func (w whereHelperenum_MemberDownloadOrderStatus) EQ(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_MemberDownloadOrderStatus) NEQ(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_MemberDownloadOrderStatus) LT(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_MemberDownloadOrderStatus) LTE(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_MemberDownloadOrderStatus) GT(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_MemberDownloadOrderStatus) GTE(x enum.MemberDownloadOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberDownloadOrderWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Status    whereHelperenum_MemberDownloadOrderStatus
	Number    whereHelperint64
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_download_order`.`id`"},
	MemberID:  whereHelperint64{field: "`member_download_order`.`member_id`"},
	Status:    whereHelperenum_MemberDownloadOrderStatus{field: "`member_download_order`.`status`"},
	Number:    whereHelperint64{field: "`member_download_order`.`number`"},
	CreatedAt: whereHelpertime_Time{field: "`member_download_order`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_download_order`.`updated_at`"},
}

// MemberDownloadOrderRels is where relationship names are stored.
var MemberDownloadOrderRels = struct {
}{}

// memberDownloadOrderR is where relationships are stored.
type memberDownloadOrderR struct {
}

// NewStruct creates a new relationship struct
func (*memberDownloadOrderR) NewStruct() *memberDownloadOrderR {
	return &memberDownloadOrderR{}
}

// memberDownloadOrderL is where Load methods for each relationship are stored.
type memberDownloadOrderL struct{}

var (
	memberDownloadOrderAllColumns            = []string{"id", "member_id", "status", "number", "created_at", "updated_at"}
	memberDownloadOrderColumnsWithoutDefault = []string{"member_id", "status", "number"}
	memberDownloadOrderColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	memberDownloadOrderPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberDownloadOrderSlice is an alias for a slice of pointers to MemberDownloadOrder.
	// This should almost always be used instead of []MemberDownloadOrder.
	MemberDownloadOrderSlice []*MemberDownloadOrder
	// MemberDownloadOrderHook is the signature for custom MemberDownloadOrder hook methods
	MemberDownloadOrderHook func(context.Context, boil.ContextExecutor, *MemberDownloadOrder) error

	memberDownloadOrderQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberDownloadOrderType                 = reflect.TypeOf(&MemberDownloadOrder{})
	memberDownloadOrderMapping              = queries.MakeStructMapping(memberDownloadOrderType)
	memberDownloadOrderPrimaryKeyMapping, _ = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, memberDownloadOrderPrimaryKeyColumns)
	memberDownloadOrderInsertCacheMut       sync.RWMutex
	memberDownloadOrderInsertCache          = make(map[string]insertCache)
	memberDownloadOrderUpdateCacheMut       sync.RWMutex
	memberDownloadOrderUpdateCache          = make(map[string]updateCache)
	memberDownloadOrderUpsertCacheMut       sync.RWMutex
	memberDownloadOrderUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberDownloadOrderBeforeInsertHooks []MemberDownloadOrderHook
var memberDownloadOrderBeforeUpdateHooks []MemberDownloadOrderHook
var memberDownloadOrderBeforeDeleteHooks []MemberDownloadOrderHook
var memberDownloadOrderBeforeUpsertHooks []MemberDownloadOrderHook

var memberDownloadOrderAfterInsertHooks []MemberDownloadOrderHook
var memberDownloadOrderAfterSelectHooks []MemberDownloadOrderHook
var memberDownloadOrderAfterUpdateHooks []MemberDownloadOrderHook
var memberDownloadOrderAfterDeleteHooks []MemberDownloadOrderHook
var memberDownloadOrderAfterUpsertHooks []MemberDownloadOrderHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberDownloadOrder) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberDownloadOrder) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberDownloadOrder) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberDownloadOrder) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberDownloadOrder) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberDownloadOrder) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberDownloadOrder) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberDownloadOrder) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberDownloadOrder) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadOrderAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberDownloadOrderHook registers your hook function for all future operations.
func AddMemberDownloadOrderHook(hookPoint boil.HookPoint, memberDownloadOrderHook MemberDownloadOrderHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberDownloadOrderBeforeInsertHooks = append(memberDownloadOrderBeforeInsertHooks, memberDownloadOrderHook)
	case boil.BeforeUpdateHook:
		memberDownloadOrderBeforeUpdateHooks = append(memberDownloadOrderBeforeUpdateHooks, memberDownloadOrderHook)
	case boil.BeforeDeleteHook:
		memberDownloadOrderBeforeDeleteHooks = append(memberDownloadOrderBeforeDeleteHooks, memberDownloadOrderHook)
	case boil.BeforeUpsertHook:
		memberDownloadOrderBeforeUpsertHooks = append(memberDownloadOrderBeforeUpsertHooks, memberDownloadOrderHook)
	case boil.AfterInsertHook:
		memberDownloadOrderAfterInsertHooks = append(memberDownloadOrderAfterInsertHooks, memberDownloadOrderHook)
	case boil.AfterSelectHook:
		memberDownloadOrderAfterSelectHooks = append(memberDownloadOrderAfterSelectHooks, memberDownloadOrderHook)
	case boil.AfterUpdateHook:
		memberDownloadOrderAfterUpdateHooks = append(memberDownloadOrderAfterUpdateHooks, memberDownloadOrderHook)
	case boil.AfterDeleteHook:
		memberDownloadOrderAfterDeleteHooks = append(memberDownloadOrderAfterDeleteHooks, memberDownloadOrderHook)
	case boil.AfterUpsertHook:
		memberDownloadOrderAfterUpsertHooks = append(memberDownloadOrderAfterUpsertHooks, memberDownloadOrderHook)
	}
}

// One returns a single memberDownloadOrder record from the query.
func (q memberDownloadOrderQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberDownloadOrder, error) {
	o := &MemberDownloadOrder{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_download_order")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberDownloadOrder records from the query.
func (q memberDownloadOrderQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberDownloadOrderSlice, error) {
	var o []*MemberDownloadOrder

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberDownloadOrder slice")
	}

	if len(memberDownloadOrderAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberDownloadOrder records in the query.
func (q memberDownloadOrderQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_download_order rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberDownloadOrderQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_download_order exists")
	}

	return count > 0, nil
}

// MemberDownloadOrders retrieves all the records using an executor.
func MemberDownloadOrders(mods ...qm.QueryMod) memberDownloadOrderQuery {
	mods = append(mods, qm.From("`member_download_order`"))
	return memberDownloadOrderQuery{NewQuery(mods...)}
}

// FindMemberDownloadOrder retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberDownloadOrder(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberDownloadOrder, error) {
	memberDownloadOrderObj := &MemberDownloadOrder{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_download_order` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberDownloadOrderObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_download_order")
	}

	if err = memberDownloadOrderObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberDownloadOrderObj, err
	}

	return memberDownloadOrderObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberDownloadOrder) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_order provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadOrderColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberDownloadOrderInsertCacheMut.RLock()
	cache, cached := memberDownloadOrderInsertCache[key]
	memberDownloadOrderInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberDownloadOrderAllColumns,
			memberDownloadOrderColumnsWithDefault,
			memberDownloadOrderColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_download_order` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_download_order` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_download_order` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberDownloadOrderPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_download_order")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberDownloadOrderMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_order")
	}

CacheNoHooks:
	if !cached {
		memberDownloadOrderInsertCacheMut.Lock()
		memberDownloadOrderInsertCache[key] = cache
		memberDownloadOrderInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberDownloadOrder.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberDownloadOrder) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberDownloadOrderUpdateCacheMut.RLock()
	cache, cached := memberDownloadOrderUpdateCache[key]
	memberDownloadOrderUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberDownloadOrderAllColumns,
			memberDownloadOrderPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_download_order, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_download_order` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberDownloadOrderPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, append(wl, memberDownloadOrderPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_download_order row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_download_order")
	}

	if !cached {
		memberDownloadOrderUpdateCacheMut.Lock()
		memberDownloadOrderUpdateCache[key] = cache
		memberDownloadOrderUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberDownloadOrderQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_download_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_download_order")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberDownloadOrderSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_download_order` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadOrderPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberDownloadOrder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberDownloadOrder")
	}
	return rowsAff, nil
}

var mySQLMemberDownloadOrderUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberDownloadOrder) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_order provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadOrderColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberDownloadOrderUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberDownloadOrderUpsertCacheMut.RLock()
	cache, cached := memberDownloadOrderUpsertCache[key]
	memberDownloadOrderUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberDownloadOrderAllColumns,
			memberDownloadOrderColumnsWithDefault,
			memberDownloadOrderColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberDownloadOrderAllColumns,
			memberDownloadOrderPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_download_order, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_download_order`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_download_order` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_download_order")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberDownloadOrderMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberDownloadOrderType, memberDownloadOrderMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_download_order")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_order")
	}

CacheNoHooks:
	if !cached {
		memberDownloadOrderUpsertCacheMut.Lock()
		memberDownloadOrderUpsertCache[key] = cache
		memberDownloadOrderUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberDownloadOrder record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberDownloadOrder) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberDownloadOrder provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberDownloadOrderPrimaryKeyMapping)
	sql := "DELETE FROM `member_download_order` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_download_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_download_order")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberDownloadOrderQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberDownloadOrderQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_download_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_order")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberDownloadOrderSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberDownloadOrderBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_download_order` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadOrderPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberDownloadOrder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_order")
	}

	if len(memberDownloadOrderAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberDownloadOrder) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberDownloadOrder(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberDownloadOrderSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberDownloadOrderSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_download_order`.* FROM `member_download_order` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadOrderPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberDownloadOrderSlice")
	}

	*o = slice

	return nil
}

// MemberDownloadOrderExists checks if the MemberDownloadOrder row exists.
func MemberDownloadOrderExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_download_order` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_download_order exists")
	}

	return exists, nil
}
