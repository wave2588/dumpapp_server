// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberDownloadIpaRecord is an object representing the database table.
type MemberDownloadIpaRecord struct {
	ID        int64       `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64       `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Status    string      `boil:"status" json:"status" toml:"status" yaml:"status"`
	IpaID     null.Int64  `boil:"ipa_id" json:"ipa_id,omitempty" toml:"ipa_id" yaml:"ipa_id,omitempty"`
	IpaType   null.String `boil:"ipa_type" json:"ipa_type,omitempty" toml:"ipa_type" yaml:"ipa_type,omitempty"`
	Version   null.String `boil:"version" json:"version,omitempty" toml:"version" yaml:"version,omitempty"`
	CreatedAt time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberDownloadIpaRecordR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberDownloadIpaRecordL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberDownloadIpaRecordColumns = struct {
	ID        string
	MemberID  string
	Status    string
	IpaID     string
	IpaType   string
	Version   string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Status:    "status",
	IpaID:     "ipa_id",
	IpaType:   "ipa_type",
	Version:   "version",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelpernull_Int64 struct{ field string }

func (w whereHelpernull_Int64) EQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Int64) NEQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Int64) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Int64) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }
func (w whereHelpernull_Int64) LT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Int64) LTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Int64) GT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Int64) GTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberDownloadIpaRecordWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Status    whereHelperstring
	IpaID     whereHelpernull_Int64
	IpaType   whereHelpernull_String
	Version   whereHelpernull_String
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_download_ipa_record`.`id`"},
	MemberID:  whereHelperint64{field: "`member_download_ipa_record`.`member_id`"},
	Status:    whereHelperstring{field: "`member_download_ipa_record`.`status`"},
	IpaID:     whereHelpernull_Int64{field: "`member_download_ipa_record`.`ipa_id`"},
	IpaType:   whereHelpernull_String{field: "`member_download_ipa_record`.`ipa_type`"},
	Version:   whereHelpernull_String{field: "`member_download_ipa_record`.`version`"},
	CreatedAt: whereHelpertime_Time{field: "`member_download_ipa_record`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_download_ipa_record`.`updated_at`"},
}

// MemberDownloadIpaRecordRels is where relationship names are stored.
var MemberDownloadIpaRecordRels = struct {
}{}

// memberDownloadIpaRecordR is where relationships are stored.
type memberDownloadIpaRecordR struct {
}

// NewStruct creates a new relationship struct
func (*memberDownloadIpaRecordR) NewStruct() *memberDownloadIpaRecordR {
	return &memberDownloadIpaRecordR{}
}

// memberDownloadIpaRecordL is where Load methods for each relationship are stored.
type memberDownloadIpaRecordL struct{}

var (
	memberDownloadIpaRecordAllColumns            = []string{"id", "member_id", "status", "ipa_id", "ipa_type", "version", "created_at", "updated_at"}
	memberDownloadIpaRecordColumnsWithoutDefault = []string{"member_id", "status", "ipa_id", "ipa_type", "version"}
	memberDownloadIpaRecordColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	memberDownloadIpaRecordPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberDownloadIpaRecordSlice is an alias for a slice of pointers to MemberDownloadIpaRecord.
	// This should almost always be used instead of []MemberDownloadIpaRecord.
	MemberDownloadIpaRecordSlice []*MemberDownloadIpaRecord
	// MemberDownloadIpaRecordHook is the signature for custom MemberDownloadIpaRecord hook methods
	MemberDownloadIpaRecordHook func(context.Context, boil.ContextExecutor, *MemberDownloadIpaRecord) error

	memberDownloadIpaRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberDownloadIpaRecordType                 = reflect.TypeOf(&MemberDownloadIpaRecord{})
	memberDownloadIpaRecordMapping              = queries.MakeStructMapping(memberDownloadIpaRecordType)
	memberDownloadIpaRecordPrimaryKeyMapping, _ = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, memberDownloadIpaRecordPrimaryKeyColumns)
	memberDownloadIpaRecordInsertCacheMut       sync.RWMutex
	memberDownloadIpaRecordInsertCache          = make(map[string]insertCache)
	memberDownloadIpaRecordUpdateCacheMut       sync.RWMutex
	memberDownloadIpaRecordUpdateCache          = make(map[string]updateCache)
	memberDownloadIpaRecordUpsertCacheMut       sync.RWMutex
	memberDownloadIpaRecordUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberDownloadIpaRecordBeforeInsertHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordBeforeUpdateHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordBeforeDeleteHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordBeforeUpsertHooks []MemberDownloadIpaRecordHook

var memberDownloadIpaRecordAfterInsertHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordAfterSelectHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordAfterUpdateHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordAfterDeleteHooks []MemberDownloadIpaRecordHook
var memberDownloadIpaRecordAfterUpsertHooks []MemberDownloadIpaRecordHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberDownloadIpaRecord) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberDownloadIpaRecord) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberDownloadIpaRecord) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberDownloadIpaRecord) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberDownloadIpaRecord) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberDownloadIpaRecord) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberDownloadIpaRecord) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberDownloadIpaRecord) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberDownloadIpaRecord) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadIpaRecordAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberDownloadIpaRecordHook registers your hook function for all future operations.
func AddMemberDownloadIpaRecordHook(hookPoint boil.HookPoint, memberDownloadIpaRecordHook MemberDownloadIpaRecordHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberDownloadIpaRecordBeforeInsertHooks = append(memberDownloadIpaRecordBeforeInsertHooks, memberDownloadIpaRecordHook)
	case boil.BeforeUpdateHook:
		memberDownloadIpaRecordBeforeUpdateHooks = append(memberDownloadIpaRecordBeforeUpdateHooks, memberDownloadIpaRecordHook)
	case boil.BeforeDeleteHook:
		memberDownloadIpaRecordBeforeDeleteHooks = append(memberDownloadIpaRecordBeforeDeleteHooks, memberDownloadIpaRecordHook)
	case boil.BeforeUpsertHook:
		memberDownloadIpaRecordBeforeUpsertHooks = append(memberDownloadIpaRecordBeforeUpsertHooks, memberDownloadIpaRecordHook)
	case boil.AfterInsertHook:
		memberDownloadIpaRecordAfterInsertHooks = append(memberDownloadIpaRecordAfterInsertHooks, memberDownloadIpaRecordHook)
	case boil.AfterSelectHook:
		memberDownloadIpaRecordAfterSelectHooks = append(memberDownloadIpaRecordAfterSelectHooks, memberDownloadIpaRecordHook)
	case boil.AfterUpdateHook:
		memberDownloadIpaRecordAfterUpdateHooks = append(memberDownloadIpaRecordAfterUpdateHooks, memberDownloadIpaRecordHook)
	case boil.AfterDeleteHook:
		memberDownloadIpaRecordAfterDeleteHooks = append(memberDownloadIpaRecordAfterDeleteHooks, memberDownloadIpaRecordHook)
	case boil.AfterUpsertHook:
		memberDownloadIpaRecordAfterUpsertHooks = append(memberDownloadIpaRecordAfterUpsertHooks, memberDownloadIpaRecordHook)
	}
}

// One returns a single memberDownloadIpaRecord record from the query.
func (q memberDownloadIpaRecordQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberDownloadIpaRecord, error) {
	o := &MemberDownloadIpaRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_download_ipa_record")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberDownloadIpaRecord records from the query.
func (q memberDownloadIpaRecordQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberDownloadIpaRecordSlice, error) {
	var o []*MemberDownloadIpaRecord

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberDownloadIpaRecord slice")
	}

	if len(memberDownloadIpaRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberDownloadIpaRecord records in the query.
func (q memberDownloadIpaRecordQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_download_ipa_record rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberDownloadIpaRecordQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_download_ipa_record exists")
	}

	return count > 0, nil
}

// MemberDownloadIpaRecords retrieves all the records using an executor.
func MemberDownloadIpaRecords(mods ...qm.QueryMod) memberDownloadIpaRecordQuery {
	mods = append(mods, qm.From("`member_download_ipa_record`"))
	return memberDownloadIpaRecordQuery{NewQuery(mods...)}
}

// FindMemberDownloadIpaRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberDownloadIpaRecord(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberDownloadIpaRecord, error) {
	memberDownloadIpaRecordObj := &MemberDownloadIpaRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_download_ipa_record` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberDownloadIpaRecordObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_download_ipa_record")
	}

	if err = memberDownloadIpaRecordObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberDownloadIpaRecordObj, err
	}

	return memberDownloadIpaRecordObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberDownloadIpaRecord) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_ipa_record provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadIpaRecordColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberDownloadIpaRecordInsertCacheMut.RLock()
	cache, cached := memberDownloadIpaRecordInsertCache[key]
	memberDownloadIpaRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberDownloadIpaRecordAllColumns,
			memberDownloadIpaRecordColumnsWithDefault,
			memberDownloadIpaRecordColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_download_ipa_record` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_download_ipa_record` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_download_ipa_record` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberDownloadIpaRecordPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_download_ipa_record")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberDownloadIpaRecordMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_ipa_record")
	}

CacheNoHooks:
	if !cached {
		memberDownloadIpaRecordInsertCacheMut.Lock()
		memberDownloadIpaRecordInsertCache[key] = cache
		memberDownloadIpaRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberDownloadIpaRecord.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberDownloadIpaRecord) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberDownloadIpaRecordUpdateCacheMut.RLock()
	cache, cached := memberDownloadIpaRecordUpdateCache[key]
	memberDownloadIpaRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberDownloadIpaRecordAllColumns,
			memberDownloadIpaRecordPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_download_ipa_record, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_download_ipa_record` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberDownloadIpaRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, append(wl, memberDownloadIpaRecordPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_download_ipa_record row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_download_ipa_record")
	}

	if !cached {
		memberDownloadIpaRecordUpdateCacheMut.Lock()
		memberDownloadIpaRecordUpdateCache[key] = cache
		memberDownloadIpaRecordUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberDownloadIpaRecordQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_download_ipa_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_download_ipa_record")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberDownloadIpaRecordSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadIpaRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_download_ipa_record` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadIpaRecordPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberDownloadIpaRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberDownloadIpaRecord")
	}
	return rowsAff, nil
}

var mySQLMemberDownloadIpaRecordUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberDownloadIpaRecord) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_ipa_record provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadIpaRecordColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberDownloadIpaRecordUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberDownloadIpaRecordUpsertCacheMut.RLock()
	cache, cached := memberDownloadIpaRecordUpsertCache[key]
	memberDownloadIpaRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberDownloadIpaRecordAllColumns,
			memberDownloadIpaRecordColumnsWithDefault,
			memberDownloadIpaRecordColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberDownloadIpaRecordAllColumns,
			memberDownloadIpaRecordPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_download_ipa_record, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_download_ipa_record`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_download_ipa_record` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_download_ipa_record")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberDownloadIpaRecordMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberDownloadIpaRecordType, memberDownloadIpaRecordMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_download_ipa_record")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_ipa_record")
	}

CacheNoHooks:
	if !cached {
		memberDownloadIpaRecordUpsertCacheMut.Lock()
		memberDownloadIpaRecordUpsertCache[key] = cache
		memberDownloadIpaRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberDownloadIpaRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberDownloadIpaRecord) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberDownloadIpaRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberDownloadIpaRecordPrimaryKeyMapping)
	sql := "DELETE FROM `member_download_ipa_record` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_download_ipa_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_download_ipa_record")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberDownloadIpaRecordQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberDownloadIpaRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_download_ipa_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_ipa_record")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberDownloadIpaRecordSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberDownloadIpaRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadIpaRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_download_ipa_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadIpaRecordPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberDownloadIpaRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_ipa_record")
	}

	if len(memberDownloadIpaRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberDownloadIpaRecord) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberDownloadIpaRecord(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberDownloadIpaRecordSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberDownloadIpaRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadIpaRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_download_ipa_record`.* FROM `member_download_ipa_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadIpaRecordPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberDownloadIpaRecordSlice")
	}

	*o = slice

	return nil
}

// MemberDownloadIpaRecordExists checks if the MemberDownloadIpaRecord row exists.
func MemberDownloadIpaRecordExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_download_ipa_record` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_download_ipa_record exists")
	}

	return exists, nil
}
