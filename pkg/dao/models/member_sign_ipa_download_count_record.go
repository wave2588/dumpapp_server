// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberSignIpaDownloadCountRecord is an object representing the database table.
type MemberSignIpaDownloadCountRecord struct {
	ID        int64                                     `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64                                     `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Type      enum.MemberSignIpaDownloadCountRecordType `boil:"type" json:"type" toml:"type" yaml:"type"`
	Count     int64                                     `boil:"count" json:"count" toml:"count" yaml:"count"`
	CreatedAt time.Time                                 `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time                                 `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberSignIpaDownloadCountRecordR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberSignIpaDownloadCountRecordL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberSignIpaDownloadCountRecordColumns = struct {
	ID        string
	MemberID  string
	Type      string
	Count     string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Type:      "type",
	Count:     "count",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelperenum_MemberSignIpaDownloadCountRecordType struct{ field string }

func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) EQ(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) NEQ(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) LT(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) LTE(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) GT(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_MemberSignIpaDownloadCountRecordType) GTE(x enum.MemberSignIpaDownloadCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberSignIpaDownloadCountRecordWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Type      whereHelperenum_MemberSignIpaDownloadCountRecordType
	Count     whereHelperint64
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_sign_ipa_download_count_record`.`id`"},
	MemberID:  whereHelperint64{field: "`member_sign_ipa_download_count_record`.`member_id`"},
	Type:      whereHelperenum_MemberSignIpaDownloadCountRecordType{field: "`member_sign_ipa_download_count_record`.`type`"},
	Count:     whereHelperint64{field: "`member_sign_ipa_download_count_record`.`count`"},
	CreatedAt: whereHelpertime_Time{field: "`member_sign_ipa_download_count_record`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_sign_ipa_download_count_record`.`updated_at`"},
}

// MemberSignIpaDownloadCountRecordRels is where relationship names are stored.
var MemberSignIpaDownloadCountRecordRels = struct {
}{}

// memberSignIpaDownloadCountRecordR is where relationships are stored.
type memberSignIpaDownloadCountRecordR struct {
}

// NewStruct creates a new relationship struct
func (*memberSignIpaDownloadCountRecordR) NewStruct() *memberSignIpaDownloadCountRecordR {
	return &memberSignIpaDownloadCountRecordR{}
}

// memberSignIpaDownloadCountRecordL is where Load methods for each relationship are stored.
type memberSignIpaDownloadCountRecordL struct{}

var (
	memberSignIpaDownloadCountRecordAllColumns            = []string{"id", "member_id", "type", "count", "created_at", "updated_at"}
	memberSignIpaDownloadCountRecordColumnsWithoutDefault = []string{"member_id", "type", "count"}
	memberSignIpaDownloadCountRecordColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	memberSignIpaDownloadCountRecordPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberSignIpaDownloadCountRecordSlice is an alias for a slice of pointers to MemberSignIpaDownloadCountRecord.
	// This should almost always be used instead of []MemberSignIpaDownloadCountRecord.
	MemberSignIpaDownloadCountRecordSlice []*MemberSignIpaDownloadCountRecord
	// MemberSignIpaDownloadCountRecordHook is the signature for custom MemberSignIpaDownloadCountRecord hook methods
	MemberSignIpaDownloadCountRecordHook func(context.Context, boil.ContextExecutor, *MemberSignIpaDownloadCountRecord) error

	memberSignIpaDownloadCountRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberSignIpaDownloadCountRecordType                 = reflect.TypeOf(&MemberSignIpaDownloadCountRecord{})
	memberSignIpaDownloadCountRecordMapping              = queries.MakeStructMapping(memberSignIpaDownloadCountRecordType)
	memberSignIpaDownloadCountRecordPrimaryKeyMapping, _ = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, memberSignIpaDownloadCountRecordPrimaryKeyColumns)
	memberSignIpaDownloadCountRecordInsertCacheMut       sync.RWMutex
	memberSignIpaDownloadCountRecordInsertCache          = make(map[string]insertCache)
	memberSignIpaDownloadCountRecordUpdateCacheMut       sync.RWMutex
	memberSignIpaDownloadCountRecordUpdateCache          = make(map[string]updateCache)
	memberSignIpaDownloadCountRecordUpsertCacheMut       sync.RWMutex
	memberSignIpaDownloadCountRecordUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberSignIpaDownloadCountRecordBeforeInsertHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordBeforeUpdateHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordBeforeDeleteHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordBeforeUpsertHooks []MemberSignIpaDownloadCountRecordHook

var memberSignIpaDownloadCountRecordAfterInsertHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordAfterSelectHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordAfterUpdateHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordAfterDeleteHooks []MemberSignIpaDownloadCountRecordHook
var memberSignIpaDownloadCountRecordAfterUpsertHooks []MemberSignIpaDownloadCountRecordHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberSignIpaDownloadCountRecord) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberSignIpaDownloadCountRecord) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberSignIpaDownloadCountRecord) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberSignIpaDownloadCountRecord) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberSignIpaDownloadCountRecord) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberSignIpaDownloadCountRecord) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberSignIpaDownloadCountRecord) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberSignIpaDownloadCountRecord) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberSignIpaDownloadCountRecord) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberSignIpaDownloadCountRecordAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberSignIpaDownloadCountRecordHook registers your hook function for all future operations.
func AddMemberSignIpaDownloadCountRecordHook(hookPoint boil.HookPoint, memberSignIpaDownloadCountRecordHook MemberSignIpaDownloadCountRecordHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberSignIpaDownloadCountRecordBeforeInsertHooks = append(memberSignIpaDownloadCountRecordBeforeInsertHooks, memberSignIpaDownloadCountRecordHook)
	case boil.BeforeUpdateHook:
		memberSignIpaDownloadCountRecordBeforeUpdateHooks = append(memberSignIpaDownloadCountRecordBeforeUpdateHooks, memberSignIpaDownloadCountRecordHook)
	case boil.BeforeDeleteHook:
		memberSignIpaDownloadCountRecordBeforeDeleteHooks = append(memberSignIpaDownloadCountRecordBeforeDeleteHooks, memberSignIpaDownloadCountRecordHook)
	case boil.BeforeUpsertHook:
		memberSignIpaDownloadCountRecordBeforeUpsertHooks = append(memberSignIpaDownloadCountRecordBeforeUpsertHooks, memberSignIpaDownloadCountRecordHook)
	case boil.AfterInsertHook:
		memberSignIpaDownloadCountRecordAfterInsertHooks = append(memberSignIpaDownloadCountRecordAfterInsertHooks, memberSignIpaDownloadCountRecordHook)
	case boil.AfterSelectHook:
		memberSignIpaDownloadCountRecordAfterSelectHooks = append(memberSignIpaDownloadCountRecordAfterSelectHooks, memberSignIpaDownloadCountRecordHook)
	case boil.AfterUpdateHook:
		memberSignIpaDownloadCountRecordAfterUpdateHooks = append(memberSignIpaDownloadCountRecordAfterUpdateHooks, memberSignIpaDownloadCountRecordHook)
	case boil.AfterDeleteHook:
		memberSignIpaDownloadCountRecordAfterDeleteHooks = append(memberSignIpaDownloadCountRecordAfterDeleteHooks, memberSignIpaDownloadCountRecordHook)
	case boil.AfterUpsertHook:
		memberSignIpaDownloadCountRecordAfterUpsertHooks = append(memberSignIpaDownloadCountRecordAfterUpsertHooks, memberSignIpaDownloadCountRecordHook)
	}
}

// One returns a single memberSignIpaDownloadCountRecord record from the query.
func (q memberSignIpaDownloadCountRecordQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberSignIpaDownloadCountRecord, error) {
	o := &MemberSignIpaDownloadCountRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_sign_ipa_download_count_record")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberSignIpaDownloadCountRecord records from the query.
func (q memberSignIpaDownloadCountRecordQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberSignIpaDownloadCountRecordSlice, error) {
	var o []*MemberSignIpaDownloadCountRecord

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberSignIpaDownloadCountRecord slice")
	}

	if len(memberSignIpaDownloadCountRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberSignIpaDownloadCountRecord records in the query.
func (q memberSignIpaDownloadCountRecordQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_sign_ipa_download_count_record rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberSignIpaDownloadCountRecordQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_sign_ipa_download_count_record exists")
	}

	return count > 0, nil
}

// MemberSignIpaDownloadCountRecords retrieves all the records using an executor.
func MemberSignIpaDownloadCountRecords(mods ...qm.QueryMod) memberSignIpaDownloadCountRecordQuery {
	mods = append(mods, qm.From("`member_sign_ipa_download_count_record`"))
	return memberSignIpaDownloadCountRecordQuery{NewQuery(mods...)}
}

// FindMemberSignIpaDownloadCountRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberSignIpaDownloadCountRecord(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberSignIpaDownloadCountRecord, error) {
	memberSignIpaDownloadCountRecordObj := &MemberSignIpaDownloadCountRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_sign_ipa_download_count_record` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberSignIpaDownloadCountRecordObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_sign_ipa_download_count_record")
	}

	if err = memberSignIpaDownloadCountRecordObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberSignIpaDownloadCountRecordObj, err
	}

	return memberSignIpaDownloadCountRecordObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberSignIpaDownloadCountRecord) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_sign_ipa_download_count_record provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberSignIpaDownloadCountRecordColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberSignIpaDownloadCountRecordInsertCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountRecordInsertCache[key]
	memberSignIpaDownloadCountRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberSignIpaDownloadCountRecordAllColumns,
			memberSignIpaDownloadCountRecordColumnsWithDefault,
			memberSignIpaDownloadCountRecordColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_sign_ipa_download_count_record` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_sign_ipa_download_count_record` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_sign_ipa_download_count_record` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberSignIpaDownloadCountRecordPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_sign_ipa_download_count_record")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberSignIpaDownloadCountRecordMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_sign_ipa_download_count_record")
	}

CacheNoHooks:
	if !cached {
		memberSignIpaDownloadCountRecordInsertCacheMut.Lock()
		memberSignIpaDownloadCountRecordInsertCache[key] = cache
		memberSignIpaDownloadCountRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberSignIpaDownloadCountRecord.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberSignIpaDownloadCountRecord) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberSignIpaDownloadCountRecordUpdateCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountRecordUpdateCache[key]
	memberSignIpaDownloadCountRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberSignIpaDownloadCountRecordAllColumns,
			memberSignIpaDownloadCountRecordPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_sign_ipa_download_count_record, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_sign_ipa_download_count_record` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberSignIpaDownloadCountRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, append(wl, memberSignIpaDownloadCountRecordPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_sign_ipa_download_count_record row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_sign_ipa_download_count_record")
	}

	if !cached {
		memberSignIpaDownloadCountRecordUpdateCacheMut.Lock()
		memberSignIpaDownloadCountRecordUpdateCache[key] = cache
		memberSignIpaDownloadCountRecordUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberSignIpaDownloadCountRecordQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_sign_ipa_download_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_sign_ipa_download_count_record")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberSignIpaDownloadCountRecordSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_sign_ipa_download_count_record` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountRecordPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberSignIpaDownloadCountRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberSignIpaDownloadCountRecord")
	}
	return rowsAff, nil
}

var mySQLMemberSignIpaDownloadCountRecordUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberSignIpaDownloadCountRecord) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_sign_ipa_download_count_record provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberSignIpaDownloadCountRecordColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberSignIpaDownloadCountRecordUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberSignIpaDownloadCountRecordUpsertCacheMut.RLock()
	cache, cached := memberSignIpaDownloadCountRecordUpsertCache[key]
	memberSignIpaDownloadCountRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberSignIpaDownloadCountRecordAllColumns,
			memberSignIpaDownloadCountRecordColumnsWithDefault,
			memberSignIpaDownloadCountRecordColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberSignIpaDownloadCountRecordAllColumns,
			memberSignIpaDownloadCountRecordPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_sign_ipa_download_count_record, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_sign_ipa_download_count_record`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_sign_ipa_download_count_record` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_sign_ipa_download_count_record")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberSignIpaDownloadCountRecordMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberSignIpaDownloadCountRecordType, memberSignIpaDownloadCountRecordMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_sign_ipa_download_count_record")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_sign_ipa_download_count_record")
	}

CacheNoHooks:
	if !cached {
		memberSignIpaDownloadCountRecordUpsertCacheMut.Lock()
		memberSignIpaDownloadCountRecordUpsertCache[key] = cache
		memberSignIpaDownloadCountRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberSignIpaDownloadCountRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberSignIpaDownloadCountRecord) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberSignIpaDownloadCountRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberSignIpaDownloadCountRecordPrimaryKeyMapping)
	sql := "DELETE FROM `member_sign_ipa_download_count_record` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_sign_ipa_download_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_sign_ipa_download_count_record")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberSignIpaDownloadCountRecordQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberSignIpaDownloadCountRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_sign_ipa_download_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_sign_ipa_download_count_record")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberSignIpaDownloadCountRecordSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberSignIpaDownloadCountRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_sign_ipa_download_count_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountRecordPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberSignIpaDownloadCountRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_sign_ipa_download_count_record")
	}

	if len(memberSignIpaDownloadCountRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberSignIpaDownloadCountRecord) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberSignIpaDownloadCountRecord(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberSignIpaDownloadCountRecordSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberSignIpaDownloadCountRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberSignIpaDownloadCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_sign_ipa_download_count_record`.* FROM `member_sign_ipa_download_count_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberSignIpaDownloadCountRecordPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberSignIpaDownloadCountRecordSlice")
	}

	*o = slice

	return nil
}

// MemberSignIpaDownloadCountRecordExists checks if the MemberSignIpaDownloadCountRecord row exists.
func MemberSignIpaDownloadCountRecordExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_sign_ipa_download_count_record` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_sign_ipa_download_count_record exists")
	}

	return exists, nil
}
