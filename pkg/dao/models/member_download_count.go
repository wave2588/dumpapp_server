// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberDownloadCount is an object representing the database table.
type MemberDownloadCount struct {
	ID        int64                          `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64                          `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Status    enum.MemberDownloadCountStatus `boil:"status" json:"status" toml:"status" yaml:"status"`
	CreatedAt time.Time                      `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time                      `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberDownloadCountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberDownloadCountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberDownloadCountColumns = struct {
	ID        string
	MemberID  string
	Status    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Status:    "status",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelperenum_MemberDownloadCountStatus struct{ field string }

func (w whereHelperenum_MemberDownloadCountStatus) EQ(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_MemberDownloadCountStatus) NEQ(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_MemberDownloadCountStatus) LT(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_MemberDownloadCountStatus) LTE(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_MemberDownloadCountStatus) GT(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_MemberDownloadCountStatus) GTE(x enum.MemberDownloadCountStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberDownloadCountWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Status    whereHelperenum_MemberDownloadCountStatus
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_download_count`.`id`"},
	MemberID:  whereHelperint64{field: "`member_download_count`.`member_id`"},
	Status:    whereHelperenum_MemberDownloadCountStatus{field: "`member_download_count`.`status`"},
	CreatedAt: whereHelpertime_Time{field: "`member_download_count`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_download_count`.`updated_at`"},
}

// MemberDownloadCountRels is where relationship names are stored.
var MemberDownloadCountRels = struct {
}{}

// memberDownloadCountR is where relationships are stored.
type memberDownloadCountR struct {
}

// NewStruct creates a new relationship struct
func (*memberDownloadCountR) NewStruct() *memberDownloadCountR {
	return &memberDownloadCountR{}
}

// memberDownloadCountL is where Load methods for each relationship are stored.
type memberDownloadCountL struct{}

var (
	memberDownloadCountAllColumns            = []string{"id", "member_id", "status", "created_at", "updated_at"}
	memberDownloadCountColumnsWithoutDefault = []string{"id", "member_id", "status"}
	memberDownloadCountColumnsWithDefault    = []string{"created_at", "updated_at"}
	memberDownloadCountPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberDownloadCountSlice is an alias for a slice of pointers to MemberDownloadCount.
	// This should almost always be used instead of []MemberDownloadCount.
	MemberDownloadCountSlice []*MemberDownloadCount
	// MemberDownloadCountHook is the signature for custom MemberDownloadCount hook methods
	MemberDownloadCountHook func(context.Context, boil.ContextExecutor, *MemberDownloadCount) error

	memberDownloadCountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberDownloadCountType                 = reflect.TypeOf(&MemberDownloadCount{})
	memberDownloadCountMapping              = queries.MakeStructMapping(memberDownloadCountType)
	memberDownloadCountPrimaryKeyMapping, _ = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, memberDownloadCountPrimaryKeyColumns)
	memberDownloadCountInsertCacheMut       sync.RWMutex
	memberDownloadCountInsertCache          = make(map[string]insertCache)
	memberDownloadCountUpdateCacheMut       sync.RWMutex
	memberDownloadCountUpdateCache          = make(map[string]updateCache)
	memberDownloadCountUpsertCacheMut       sync.RWMutex
	memberDownloadCountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberDownloadCountBeforeInsertHooks []MemberDownloadCountHook
var memberDownloadCountBeforeUpdateHooks []MemberDownloadCountHook
var memberDownloadCountBeforeDeleteHooks []MemberDownloadCountHook
var memberDownloadCountBeforeUpsertHooks []MemberDownloadCountHook

var memberDownloadCountAfterInsertHooks []MemberDownloadCountHook
var memberDownloadCountAfterSelectHooks []MemberDownloadCountHook
var memberDownloadCountAfterUpdateHooks []MemberDownloadCountHook
var memberDownloadCountAfterDeleteHooks []MemberDownloadCountHook
var memberDownloadCountAfterUpsertHooks []MemberDownloadCountHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberDownloadCount) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberDownloadCount) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberDownloadCount) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberDownloadCount) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberDownloadCount) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberDownloadCount) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberDownloadCount) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberDownloadCount) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberDownloadCount) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberDownloadCountAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberDownloadCountHook registers your hook function for all future operations.
func AddMemberDownloadCountHook(hookPoint boil.HookPoint, memberDownloadCountHook MemberDownloadCountHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberDownloadCountBeforeInsertHooks = append(memberDownloadCountBeforeInsertHooks, memberDownloadCountHook)
	case boil.BeforeUpdateHook:
		memberDownloadCountBeforeUpdateHooks = append(memberDownloadCountBeforeUpdateHooks, memberDownloadCountHook)
	case boil.BeforeDeleteHook:
		memberDownloadCountBeforeDeleteHooks = append(memberDownloadCountBeforeDeleteHooks, memberDownloadCountHook)
	case boil.BeforeUpsertHook:
		memberDownloadCountBeforeUpsertHooks = append(memberDownloadCountBeforeUpsertHooks, memberDownloadCountHook)
	case boil.AfterInsertHook:
		memberDownloadCountAfterInsertHooks = append(memberDownloadCountAfterInsertHooks, memberDownloadCountHook)
	case boil.AfterSelectHook:
		memberDownloadCountAfterSelectHooks = append(memberDownloadCountAfterSelectHooks, memberDownloadCountHook)
	case boil.AfterUpdateHook:
		memberDownloadCountAfterUpdateHooks = append(memberDownloadCountAfterUpdateHooks, memberDownloadCountHook)
	case boil.AfterDeleteHook:
		memberDownloadCountAfterDeleteHooks = append(memberDownloadCountAfterDeleteHooks, memberDownloadCountHook)
	case boil.AfterUpsertHook:
		memberDownloadCountAfterUpsertHooks = append(memberDownloadCountAfterUpsertHooks, memberDownloadCountHook)
	}
}

// One returns a single memberDownloadCount record from the query.
func (q memberDownloadCountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberDownloadCount, error) {
	o := &MemberDownloadCount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_download_count")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberDownloadCount records from the query.
func (q memberDownloadCountQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberDownloadCountSlice, error) {
	var o []*MemberDownloadCount

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberDownloadCount slice")
	}

	if len(memberDownloadCountAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberDownloadCount records in the query.
func (q memberDownloadCountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_download_count rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberDownloadCountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_download_count exists")
	}

	return count > 0, nil
}

// MemberDownloadCounts retrieves all the records using an executor.
func MemberDownloadCounts(mods ...qm.QueryMod) memberDownloadCountQuery {
	mods = append(mods, qm.From("`member_download_count`"))
	return memberDownloadCountQuery{NewQuery(mods...)}
}

// FindMemberDownloadCount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberDownloadCount(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberDownloadCount, error) {
	memberDownloadCountObj := &MemberDownloadCount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_download_count` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberDownloadCountObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_download_count")
	}

	if err = memberDownloadCountObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberDownloadCountObj, err
	}

	return memberDownloadCountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberDownloadCount) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_count provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadCountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberDownloadCountInsertCacheMut.RLock()
	cache, cached := memberDownloadCountInsertCache[key]
	memberDownloadCountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberDownloadCountAllColumns,
			memberDownloadCountColumnsWithDefault,
			memberDownloadCountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_download_count` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_download_count` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_download_count` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberDownloadCountPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_download_count")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_count")
	}

CacheNoHooks:
	if !cached {
		memberDownloadCountInsertCacheMut.Lock()
		memberDownloadCountInsertCache[key] = cache
		memberDownloadCountInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberDownloadCount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberDownloadCount) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberDownloadCountUpdateCacheMut.RLock()
	cache, cached := memberDownloadCountUpdateCache[key]
	memberDownloadCountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberDownloadCountAllColumns,
			memberDownloadCountPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_download_count, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_download_count` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberDownloadCountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, append(wl, memberDownloadCountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_download_count row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_download_count")
	}

	if !cached {
		memberDownloadCountUpdateCacheMut.Lock()
		memberDownloadCountUpdateCache[key] = cache
		memberDownloadCountUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberDownloadCountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_download_count")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberDownloadCountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_download_count` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadCountPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberDownloadCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberDownloadCount")
	}
	return rowsAff, nil
}

var mySQLMemberDownloadCountUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberDownloadCount) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_download_count provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberDownloadCountColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberDownloadCountUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberDownloadCountUpsertCacheMut.RLock()
	cache, cached := memberDownloadCountUpsertCache[key]
	memberDownloadCountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberDownloadCountAllColumns,
			memberDownloadCountColumnsWithDefault,
			memberDownloadCountColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberDownloadCountAllColumns,
			memberDownloadCountPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_download_count, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_download_count`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_download_count` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_download_count")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberDownloadCountType, memberDownloadCountMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_download_count")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_download_count")
	}

CacheNoHooks:
	if !cached {
		memberDownloadCountUpsertCacheMut.Lock()
		memberDownloadCountUpsertCache[key] = cache
		memberDownloadCountUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberDownloadCount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberDownloadCount) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberDownloadCount provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberDownloadCountPrimaryKeyMapping)
	sql := "DELETE FROM `member_download_count` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_download_count")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberDownloadCountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberDownloadCountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_download_count")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_count")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberDownloadCountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberDownloadCountBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_download_count` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadCountPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberDownloadCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_download_count")
	}

	if len(memberDownloadCountAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberDownloadCount) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberDownloadCount(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberDownloadCountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberDownloadCountSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberDownloadCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_download_count`.* FROM `member_download_count` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberDownloadCountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberDownloadCountSlice")
	}

	*o = slice

	return nil
}

// MemberDownloadCountExists checks if the MemberDownloadCount row exists.
func MemberDownloadCountExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_download_count` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_download_count exists")
	}

	return exists, nil
}
