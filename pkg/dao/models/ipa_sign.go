// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// IpaSign is an object representing the database table.
type IpaSign struct {
	ID            int64              `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	IpaID         int64              `boil:"ipa_id" json:"ipa_id" toml:"ipa_id" yaml:"ipa_id"`
	CertificateID int64              `boil:"certificate_id" json:"certificate_id" toml:"certificate_id" yaml:"certificate_id"`
	MemberID      int64              `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Status        enum.IpaSignStatus `boil:"status" json:"status" toml:"status" yaml:"status"`
	TokenPath     null.String        `boil:"token_path" json:"token_path,omitempty" toml:"token_path" yaml:"token_path,omitempty"`
	BizExt        string             `boil:"biz_ext" json:"biz_ext" toml:"biz_ext" yaml:"biz_ext"`
	CreatedAt     time.Time          `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt     time.Time          `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *ipaSignR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ipaSignL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var IpaSignColumns = struct {
	ID            string
	IpaID         string
	CertificateID string
	MemberID      string
	Status        string
	TokenPath     string
	BizExt        string
	CreatedAt     string
	UpdatedAt     string
}{
	ID:            "id",
	IpaID:         "ipa_id",
	CertificateID: "certificate_id",
	MemberID:      "member_id",
	Status:        "status",
	TokenPath:     "token_path",
	BizExt:        "biz_ext",
	CreatedAt:     "created_at",
	UpdatedAt:     "updated_at",
}

// Generated where

type whereHelperenum_IpaSignStatus struct{ field string }

func (w whereHelperenum_IpaSignStatus) EQ(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_IpaSignStatus) NEQ(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_IpaSignStatus) LT(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_IpaSignStatus) LTE(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_IpaSignStatus) GT(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_IpaSignStatus) GTE(x enum.IpaSignStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var IpaSignWhere = struct {
	ID            whereHelperint64
	IpaID         whereHelperint64
	CertificateID whereHelperint64
	MemberID      whereHelperint64
	Status        whereHelperenum_IpaSignStatus
	TokenPath     whereHelpernull_String
	BizExt        whereHelperstring
	CreatedAt     whereHelpertime_Time
	UpdatedAt     whereHelpertime_Time
}{
	ID:            whereHelperint64{field: "`ipa_sign`.`id`"},
	IpaID:         whereHelperint64{field: "`ipa_sign`.`ipa_id`"},
	CertificateID: whereHelperint64{field: "`ipa_sign`.`certificate_id`"},
	MemberID:      whereHelperint64{field: "`ipa_sign`.`member_id`"},
	Status:        whereHelperenum_IpaSignStatus{field: "`ipa_sign`.`status`"},
	TokenPath:     whereHelpernull_String{field: "`ipa_sign`.`token_path`"},
	BizExt:        whereHelperstring{field: "`ipa_sign`.`biz_ext`"},
	CreatedAt:     whereHelpertime_Time{field: "`ipa_sign`.`created_at`"},
	UpdatedAt:     whereHelpertime_Time{field: "`ipa_sign`.`updated_at`"},
}

// IpaSignRels is where relationship names are stored.
var IpaSignRels = struct {
}{}

// ipaSignR is where relationships are stored.
type ipaSignR struct {
}

// NewStruct creates a new relationship struct
func (*ipaSignR) NewStruct() *ipaSignR {
	return &ipaSignR{}
}

// ipaSignL is where Load methods for each relationship are stored.
type ipaSignL struct{}

var (
	ipaSignAllColumns            = []string{"id", "ipa_id", "certificate_id", "member_id", "status", "token_path", "biz_ext", "created_at", "updated_at"}
	ipaSignColumnsWithoutDefault = []string{"ipa_id", "certificate_id", "member_id", "status", "token_path", "biz_ext"}
	ipaSignColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	ipaSignPrimaryKeyColumns     = []string{"id"}
)

type (
	// IpaSignSlice is an alias for a slice of pointers to IpaSign.
	// This should almost always be used instead of []IpaSign.
	IpaSignSlice []*IpaSign
	// IpaSignHook is the signature for custom IpaSign hook methods
	IpaSignHook func(context.Context, boil.ContextExecutor, *IpaSign) error

	ipaSignQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ipaSignType                 = reflect.TypeOf(&IpaSign{})
	ipaSignMapping              = queries.MakeStructMapping(ipaSignType)
	ipaSignPrimaryKeyMapping, _ = queries.BindMapping(ipaSignType, ipaSignMapping, ipaSignPrimaryKeyColumns)
	ipaSignInsertCacheMut       sync.RWMutex
	ipaSignInsertCache          = make(map[string]insertCache)
	ipaSignUpdateCacheMut       sync.RWMutex
	ipaSignUpdateCache          = make(map[string]updateCache)
	ipaSignUpsertCacheMut       sync.RWMutex
	ipaSignUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ipaSignBeforeInsertHooks []IpaSignHook
var ipaSignBeforeUpdateHooks []IpaSignHook
var ipaSignBeforeDeleteHooks []IpaSignHook
var ipaSignBeforeUpsertHooks []IpaSignHook

var ipaSignAfterInsertHooks []IpaSignHook
var ipaSignAfterSelectHooks []IpaSignHook
var ipaSignAfterUpdateHooks []IpaSignHook
var ipaSignAfterDeleteHooks []IpaSignHook
var ipaSignAfterUpsertHooks []IpaSignHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *IpaSign) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *IpaSign) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *IpaSign) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *IpaSign) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *IpaSign) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *IpaSign) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *IpaSign) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *IpaSign) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *IpaSign) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ipaSignAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddIpaSignHook registers your hook function for all future operations.
func AddIpaSignHook(hookPoint boil.HookPoint, ipaSignHook IpaSignHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		ipaSignBeforeInsertHooks = append(ipaSignBeforeInsertHooks, ipaSignHook)
	case boil.BeforeUpdateHook:
		ipaSignBeforeUpdateHooks = append(ipaSignBeforeUpdateHooks, ipaSignHook)
	case boil.BeforeDeleteHook:
		ipaSignBeforeDeleteHooks = append(ipaSignBeforeDeleteHooks, ipaSignHook)
	case boil.BeforeUpsertHook:
		ipaSignBeforeUpsertHooks = append(ipaSignBeforeUpsertHooks, ipaSignHook)
	case boil.AfterInsertHook:
		ipaSignAfterInsertHooks = append(ipaSignAfterInsertHooks, ipaSignHook)
	case boil.AfterSelectHook:
		ipaSignAfterSelectHooks = append(ipaSignAfterSelectHooks, ipaSignHook)
	case boil.AfterUpdateHook:
		ipaSignAfterUpdateHooks = append(ipaSignAfterUpdateHooks, ipaSignHook)
	case boil.AfterDeleteHook:
		ipaSignAfterDeleteHooks = append(ipaSignAfterDeleteHooks, ipaSignHook)
	case boil.AfterUpsertHook:
		ipaSignAfterUpsertHooks = append(ipaSignAfterUpsertHooks, ipaSignHook)
	}
}

// One returns a single ipaSign record from the query.
func (q ipaSignQuery) One(ctx context.Context, exec boil.ContextExecutor) (*IpaSign, error) {
	o := &IpaSign{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ipa_sign")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all IpaSign records from the query.
func (q ipaSignQuery) All(ctx context.Context, exec boil.ContextExecutor) (IpaSignSlice, error) {
	var o []*IpaSign

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to IpaSign slice")
	}

	if len(ipaSignAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all IpaSign records in the query.
func (q ipaSignQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ipa_sign rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ipaSignQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ipa_sign exists")
	}

	return count > 0, nil
}

// IpaSigns retrieves all the records using an executor.
func IpaSigns(mods ...qm.QueryMod) ipaSignQuery {
	mods = append(mods, qm.From("`ipa_sign`"))
	return ipaSignQuery{NewQuery(mods...)}
}

// FindIpaSign retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindIpaSign(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*IpaSign, error) {
	ipaSignObj := &IpaSign{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ipa_sign` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, ipaSignObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ipa_sign")
	}

	if err = ipaSignObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ipaSignObj, err
	}

	return ipaSignObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *IpaSign) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ipa_sign provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ipaSignColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ipaSignInsertCacheMut.RLock()
	cache, cached := ipaSignInsertCache[key]
	ipaSignInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ipaSignAllColumns,
			ipaSignColumnsWithDefault,
			ipaSignColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ipaSignType, ipaSignMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ipaSignType, ipaSignMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ipa_sign` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ipa_sign` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ipa_sign` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ipaSignPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ipa_sign")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == ipaSignMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ipa_sign")
	}

CacheNoHooks:
	if !cached {
		ipaSignInsertCacheMut.Lock()
		ipaSignInsertCache[key] = cache
		ipaSignInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the IpaSign.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *IpaSign) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ipaSignUpdateCacheMut.RLock()
	cache, cached := ipaSignUpdateCache[key]
	ipaSignUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ipaSignAllColumns,
			ipaSignPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ipa_sign, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ipa_sign` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ipaSignPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ipaSignType, ipaSignMapping, append(wl, ipaSignPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ipa_sign row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ipa_sign")
	}

	if !cached {
		ipaSignUpdateCacheMut.Lock()
		ipaSignUpdateCache[key] = cache
		ipaSignUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ipaSignQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ipa_sign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ipa_sign")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o IpaSignSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaSignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ipa_sign` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaSignPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ipaSign slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ipaSign")
	}
	return rowsAff, nil
}

var mySQLIpaSignUniqueColumns = []string{
	"id",
	"token_path",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *IpaSign) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ipa_sign provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ipaSignColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLIpaSignUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ipaSignUpsertCacheMut.RLock()
	cache, cached := ipaSignUpsertCache[key]
	ipaSignUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			ipaSignAllColumns,
			ipaSignColumnsWithDefault,
			ipaSignColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			ipaSignAllColumns,
			ipaSignPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ipa_sign, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`ipa_sign`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ipa_sign` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ipaSignType, ipaSignMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ipaSignType, ipaSignMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ipa_sign")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == ipaSignMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ipaSignType, ipaSignMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ipa_sign")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ipa_sign")
	}

CacheNoHooks:
	if !cached {
		ipaSignUpsertCacheMut.Lock()
		ipaSignUpsertCache[key] = cache
		ipaSignUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single IpaSign record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *IpaSign) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no IpaSign provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ipaSignPrimaryKeyMapping)
	sql := "DELETE FROM `ipa_sign` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ipa_sign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ipa_sign")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ipaSignQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ipaSignQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ipa_sign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ipa_sign")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o IpaSignSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ipaSignBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaSignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ipa_sign` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaSignPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ipaSign slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ipa_sign")
	}

	if len(ipaSignAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *IpaSign) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindIpaSign(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *IpaSignSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := IpaSignSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ipaSignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ipa_sign`.* FROM `ipa_sign` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ipaSignPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in IpaSignSlice")
	}

	*o = slice

	return nil
}

// IpaSignExists checks if the IpaSign row exists.
func IpaSignExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ipa_sign` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ipa_sign exists")
	}

	return exists, nil
}
