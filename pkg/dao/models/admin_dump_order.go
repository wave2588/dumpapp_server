// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AdminDumpOrder is an object representing the database table.
type AdminDumpOrder struct {
	ID         int64                     `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	DemanderID int64                     `boil:"demander_id" json:"demander_id" toml:"demander_id" yaml:"demander_id"`
	OperatorID int64                     `boil:"operator_id" json:"operator_id" toml:"operator_id" yaml:"operator_id"`
	IpaID      int64                     `boil:"ipa_id" json:"ipa_id" toml:"ipa_id" yaml:"ipa_id"`
	IpaVersion string                    `boil:"ipa_version" json:"ipa_version" toml:"ipa_version" yaml:"ipa_version"`
	IpaBizExt  string                    `boil:"ipa_biz_ext" json:"ipa_biz_ext" toml:"ipa_biz_ext" yaml:"ipa_biz_ext"`
	Status     enum.AdminDumpOrderStatus `boil:"status" json:"status" toml:"status" yaml:"status"`
	// ????
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	// ????
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *adminDumpOrderR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L adminDumpOrderL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AdminDumpOrderColumns = struct {
	ID         string
	DemanderID string
	OperatorID string
	IpaID      string
	IpaVersion string
	IpaBizExt  string
	Status     string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	DemanderID: "demander_id",
	OperatorID: "operator_id",
	IpaID:      "ipa_id",
	IpaVersion: "ipa_version",
	IpaBizExt:  "ipa_biz_ext",
	Status:     "status",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

var AdminDumpOrderTableColumns = struct {
	ID         string
	DemanderID string
	OperatorID string
	IpaID      string
	IpaVersion string
	IpaBizExt  string
	Status     string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "admin_dump_order.id",
	DemanderID: "admin_dump_order.demander_id",
	OperatorID: "admin_dump_order.operator_id",
	IpaID:      "admin_dump_order.ipa_id",
	IpaVersion: "admin_dump_order.ipa_version",
	IpaBizExt:  "admin_dump_order.ipa_biz_ext",
	Status:     "admin_dump_order.status",
	CreatedAt:  "admin_dump_order.created_at",
	UpdatedAt:  "admin_dump_order.updated_at",
}

// Generated where

type whereHelperenum_AdminDumpOrderStatus struct{ field string }

func (w whereHelperenum_AdminDumpOrderStatus) EQ(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_AdminDumpOrderStatus) NEQ(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_AdminDumpOrderStatus) LT(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_AdminDumpOrderStatus) LTE(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_AdminDumpOrderStatus) GT(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_AdminDumpOrderStatus) GTE(x enum.AdminDumpOrderStatus) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var AdminDumpOrderWhere = struct {
	ID         whereHelperint64
	DemanderID whereHelperint64
	OperatorID whereHelperint64
	IpaID      whereHelperint64
	IpaVersion whereHelperstring
	IpaBizExt  whereHelperstring
	Status     whereHelperenum_AdminDumpOrderStatus
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
}{
	ID:         whereHelperint64{field: "`admin_dump_order`.`id`"},
	DemanderID: whereHelperint64{field: "`admin_dump_order`.`demander_id`"},
	OperatorID: whereHelperint64{field: "`admin_dump_order`.`operator_id`"},
	IpaID:      whereHelperint64{field: "`admin_dump_order`.`ipa_id`"},
	IpaVersion: whereHelperstring{field: "`admin_dump_order`.`ipa_version`"},
	IpaBizExt:  whereHelperstring{field: "`admin_dump_order`.`ipa_biz_ext`"},
	Status:     whereHelperenum_AdminDumpOrderStatus{field: "`admin_dump_order`.`status`"},
	CreatedAt:  whereHelpertime_Time{field: "`admin_dump_order`.`created_at`"},
	UpdatedAt:  whereHelpertime_Time{field: "`admin_dump_order`.`updated_at`"},
}

// AdminDumpOrderRels is where relationship names are stored.
var AdminDumpOrderRels = struct {
}{}

// adminDumpOrderR is where relationships are stored.
type adminDumpOrderR struct {
}

// NewStruct creates a new relationship struct
func (*adminDumpOrderR) NewStruct() *adminDumpOrderR {
	return &adminDumpOrderR{}
}

// adminDumpOrderL is where Load methods for each relationship are stored.
type adminDumpOrderL struct{}

var (
	adminDumpOrderAllColumns            = []string{"id", "demander_id", "operator_id", "ipa_id", "ipa_version", "ipa_biz_ext", "status", "created_at", "updated_at"}
	adminDumpOrderColumnsWithoutDefault = []string{"demander_id", "operator_id", "ipa_id", "ipa_version", "ipa_biz_ext", "status"}
	adminDumpOrderColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	adminDumpOrderPrimaryKeyColumns     = []string{"id"}
	adminDumpOrderGeneratedColumns      = []string{}
)

type (
	// AdminDumpOrderSlice is an alias for a slice of pointers to AdminDumpOrder.
	// This should almost always be used instead of []AdminDumpOrder.
	AdminDumpOrderSlice []*AdminDumpOrder
	// AdminDumpOrderHook is the signature for custom AdminDumpOrder hook methods
	AdminDumpOrderHook func(context.Context, boil.ContextExecutor, *AdminDumpOrder) error

	adminDumpOrderQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	adminDumpOrderType                 = reflect.TypeOf(&AdminDumpOrder{})
	adminDumpOrderMapping              = queries.MakeStructMapping(adminDumpOrderType)
	adminDumpOrderPrimaryKeyMapping, _ = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, adminDumpOrderPrimaryKeyColumns)
	adminDumpOrderInsertCacheMut       sync.RWMutex
	adminDumpOrderInsertCache          = make(map[string]insertCache)
	adminDumpOrderUpdateCacheMut       sync.RWMutex
	adminDumpOrderUpdateCache          = make(map[string]updateCache)
	adminDumpOrderUpsertCacheMut       sync.RWMutex
	adminDumpOrderUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var adminDumpOrderAfterSelectHooks []AdminDumpOrderHook

var adminDumpOrderBeforeInsertHooks []AdminDumpOrderHook
var adminDumpOrderAfterInsertHooks []AdminDumpOrderHook

var adminDumpOrderBeforeUpdateHooks []AdminDumpOrderHook
var adminDumpOrderAfterUpdateHooks []AdminDumpOrderHook

var adminDumpOrderBeforeDeleteHooks []AdminDumpOrderHook
var adminDumpOrderAfterDeleteHooks []AdminDumpOrderHook

var adminDumpOrderBeforeUpsertHooks []AdminDumpOrderHook
var adminDumpOrderAfterUpsertHooks []AdminDumpOrderHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AdminDumpOrder) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AdminDumpOrder) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AdminDumpOrder) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AdminDumpOrder) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AdminDumpOrder) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AdminDumpOrder) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AdminDumpOrder) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AdminDumpOrder) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AdminDumpOrder) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminDumpOrderAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAdminDumpOrderHook registers your hook function for all future operations.
func AddAdminDumpOrderHook(hookPoint boil.HookPoint, adminDumpOrderHook AdminDumpOrderHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		adminDumpOrderAfterSelectHooks = append(adminDumpOrderAfterSelectHooks, adminDumpOrderHook)
	case boil.BeforeInsertHook:
		adminDumpOrderBeforeInsertHooks = append(adminDumpOrderBeforeInsertHooks, adminDumpOrderHook)
	case boil.AfterInsertHook:
		adminDumpOrderAfterInsertHooks = append(adminDumpOrderAfterInsertHooks, adminDumpOrderHook)
	case boil.BeforeUpdateHook:
		adminDumpOrderBeforeUpdateHooks = append(adminDumpOrderBeforeUpdateHooks, adminDumpOrderHook)
	case boil.AfterUpdateHook:
		adminDumpOrderAfterUpdateHooks = append(adminDumpOrderAfterUpdateHooks, adminDumpOrderHook)
	case boil.BeforeDeleteHook:
		adminDumpOrderBeforeDeleteHooks = append(adminDumpOrderBeforeDeleteHooks, adminDumpOrderHook)
	case boil.AfterDeleteHook:
		adminDumpOrderAfterDeleteHooks = append(adminDumpOrderAfterDeleteHooks, adminDumpOrderHook)
	case boil.BeforeUpsertHook:
		adminDumpOrderBeforeUpsertHooks = append(adminDumpOrderBeforeUpsertHooks, adminDumpOrderHook)
	case boil.AfterUpsertHook:
		adminDumpOrderAfterUpsertHooks = append(adminDumpOrderAfterUpsertHooks, adminDumpOrderHook)
	}
}

// One returns a single adminDumpOrder record from the query.
func (q adminDumpOrderQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AdminDumpOrder, error) {
	o := &AdminDumpOrder{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for admin_dump_order")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AdminDumpOrder records from the query.
func (q adminDumpOrderQuery) All(ctx context.Context, exec boil.ContextExecutor) (AdminDumpOrderSlice, error) {
	var o []*AdminDumpOrder

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AdminDumpOrder slice")
	}

	if len(adminDumpOrderAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AdminDumpOrder records in the query.
func (q adminDumpOrderQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count admin_dump_order rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q adminDumpOrderQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if admin_dump_order exists")
	}

	return count > 0, nil
}

// AdminDumpOrders retrieves all the records using an executor.
func AdminDumpOrders(mods ...qm.QueryMod) adminDumpOrderQuery {
	mods = append(mods, qm.From("`admin_dump_order`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`admin_dump_order`.*"})
	}

	return adminDumpOrderQuery{q}
}

// FindAdminDumpOrder retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAdminDumpOrder(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*AdminDumpOrder, error) {
	adminDumpOrderObj := &AdminDumpOrder{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `admin_dump_order` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, adminDumpOrderObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from admin_dump_order")
	}

	if err = adminDumpOrderObj.doAfterSelectHooks(ctx, exec); err != nil {
		return adminDumpOrderObj, err
	}

	return adminDumpOrderObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AdminDumpOrder) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no admin_dump_order provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(adminDumpOrderColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	adminDumpOrderInsertCacheMut.RLock()
	cache, cached := adminDumpOrderInsertCache[key]
	adminDumpOrderInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			adminDumpOrderAllColumns,
			adminDumpOrderColumnsWithDefault,
			adminDumpOrderColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `admin_dump_order` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `admin_dump_order` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `admin_dump_order` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, adminDumpOrderPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into admin_dump_order")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == adminDumpOrderMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for admin_dump_order")
	}

CacheNoHooks:
	if !cached {
		adminDumpOrderInsertCacheMut.Lock()
		adminDumpOrderInsertCache[key] = cache
		adminDumpOrderInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AdminDumpOrder.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AdminDumpOrder) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	adminDumpOrderUpdateCacheMut.RLock()
	cache, cached := adminDumpOrderUpdateCache[key]
	adminDumpOrderUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			adminDumpOrderAllColumns,
			adminDumpOrderPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update admin_dump_order, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `admin_dump_order` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, adminDumpOrderPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, append(wl, adminDumpOrderPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update admin_dump_order row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for admin_dump_order")
	}

	if !cached {
		adminDumpOrderUpdateCacheMut.Lock()
		adminDumpOrderUpdateCache[key] = cache
		adminDumpOrderUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q adminDumpOrderQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for admin_dump_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for admin_dump_order")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AdminDumpOrderSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminDumpOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `admin_dump_order` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminDumpOrderPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in adminDumpOrder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all adminDumpOrder")
	}
	return rowsAff, nil
}

var mySQLAdminDumpOrderUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AdminDumpOrder) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no admin_dump_order provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(adminDumpOrderColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAdminDumpOrderUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	adminDumpOrderUpsertCacheMut.RLock()
	cache, cached := adminDumpOrderUpsertCache[key]
	adminDumpOrderUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			adminDumpOrderAllColumns,
			adminDumpOrderColumnsWithDefault,
			adminDumpOrderColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			adminDumpOrderAllColumns,
			adminDumpOrderPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert admin_dump_order, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`admin_dump_order`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `admin_dump_order` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for admin_dump_order")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == adminDumpOrderMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(adminDumpOrderType, adminDumpOrderMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for admin_dump_order")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for admin_dump_order")
	}

CacheNoHooks:
	if !cached {
		adminDumpOrderUpsertCacheMut.Lock()
		adminDumpOrderUpsertCache[key] = cache
		adminDumpOrderUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AdminDumpOrder record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AdminDumpOrder) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AdminDumpOrder provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), adminDumpOrderPrimaryKeyMapping)
	sql := "DELETE FROM `admin_dump_order` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from admin_dump_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for admin_dump_order")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q adminDumpOrderQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no adminDumpOrderQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from admin_dump_order")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for admin_dump_order")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AdminDumpOrderSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(adminDumpOrderBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminDumpOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `admin_dump_order` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminDumpOrderPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from adminDumpOrder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for admin_dump_order")
	}

	if len(adminDumpOrderAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AdminDumpOrder) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAdminDumpOrder(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AdminDumpOrderSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AdminDumpOrderSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminDumpOrderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `admin_dump_order`.* FROM `admin_dump_order` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminDumpOrderPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AdminDumpOrderSlice")
	}

	*o = slice

	return nil
}

// AdminDumpOrderExists checks if the AdminDumpOrder row exists.
func AdminDumpOrderExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `admin_dump_order` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if admin_dump_order exists")
	}

	return exists, nil
}

// Exists checks if the AdminDumpOrder row exists.
func (o *AdminDumpOrder) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return AdminDumpOrderExists(ctx, exec, o.ID)
}
