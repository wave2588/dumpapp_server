// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"dumpapp_server/pkg/common/datatype"
	"dumpapp_server/pkg/common/enum"
	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MemberPayCountRecord is an object representing the database table.
type MemberPayCountRecord struct {
	ID        int64                               `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID  int64                               `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Type      enum.MemberPayCountRecordType       `boil:"type" json:"type" toml:"type" yaml:"type"`
	Count     int64                               `boil:"count" json:"count" toml:"count" yaml:"count"`
	BizExt    datatype.MemberPayCountRecordBizExt `boil:"biz_ext" json:"biz_ext" toml:"biz_ext" yaml:"biz_ext"`
	CreatedAt time.Time                           `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time                           `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *memberPayCountRecordR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L memberPayCountRecordL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MemberPayCountRecordColumns = struct {
	ID        string
	MemberID  string
	Type      string
	Count     string
	BizExt    string
	CreatedAt string
	UpdatedAt string
}{
	ID:        "id",
	MemberID:  "member_id",
	Type:      "type",
	Count:     "count",
	BizExt:    "biz_ext",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
}

// Generated where

type whereHelperenum_MemberPayCountRecordType struct{ field string }

func (w whereHelperenum_MemberPayCountRecordType) EQ(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperenum_MemberPayCountRecordType) NEQ(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperenum_MemberPayCountRecordType) LT(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperenum_MemberPayCountRecordType) LTE(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperenum_MemberPayCountRecordType) GT(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperenum_MemberPayCountRecordType) GTE(x enum.MemberPayCountRecordType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelperdatatype_MemberPayCountRecordBizExt struct{ field string }

func (w whereHelperdatatype_MemberPayCountRecordBizExt) EQ(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperdatatype_MemberPayCountRecordBizExt) NEQ(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperdatatype_MemberPayCountRecordBizExt) LT(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperdatatype_MemberPayCountRecordBizExt) LTE(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperdatatype_MemberPayCountRecordBizExt) GT(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperdatatype_MemberPayCountRecordBizExt) GTE(x datatype.MemberPayCountRecordBizExt) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MemberPayCountRecordWhere = struct {
	ID        whereHelperint64
	MemberID  whereHelperint64
	Type      whereHelperenum_MemberPayCountRecordType
	Count     whereHelperint64
	BizExt    whereHelperdatatype_MemberPayCountRecordBizExt
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "`member_pay_count_record`.`id`"},
	MemberID:  whereHelperint64{field: "`member_pay_count_record`.`member_id`"},
	Type:      whereHelperenum_MemberPayCountRecordType{field: "`member_pay_count_record`.`type`"},
	Count:     whereHelperint64{field: "`member_pay_count_record`.`count`"},
	BizExt:    whereHelperdatatype_MemberPayCountRecordBizExt{field: "`member_pay_count_record`.`biz_ext`"},
	CreatedAt: whereHelpertime_Time{field: "`member_pay_count_record`.`created_at`"},
	UpdatedAt: whereHelpertime_Time{field: "`member_pay_count_record`.`updated_at`"},
}

// MemberPayCountRecordRels is where relationship names are stored.
var MemberPayCountRecordRels = struct {
}{}

// memberPayCountRecordR is where relationships are stored.
type memberPayCountRecordR struct {
}

// NewStruct creates a new relationship struct
func (*memberPayCountRecordR) NewStruct() *memberPayCountRecordR {
	return &memberPayCountRecordR{}
}

// memberPayCountRecordL is where Load methods for each relationship are stored.
type memberPayCountRecordL struct{}

var (
	memberPayCountRecordAllColumns            = []string{"id", "member_id", "type", "count", "biz_ext", "created_at", "updated_at"}
	memberPayCountRecordColumnsWithoutDefault = []string{"member_id", "type", "count", "biz_ext"}
	memberPayCountRecordColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	memberPayCountRecordPrimaryKeyColumns     = []string{"id"}
)

type (
	// MemberPayCountRecordSlice is an alias for a slice of pointers to MemberPayCountRecord.
	// This should almost always be used instead of []MemberPayCountRecord.
	MemberPayCountRecordSlice []*MemberPayCountRecord
	// MemberPayCountRecordHook is the signature for custom MemberPayCountRecord hook methods
	MemberPayCountRecordHook func(context.Context, boil.ContextExecutor, *MemberPayCountRecord) error

	memberPayCountRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	memberPayCountRecordType                 = reflect.TypeOf(&MemberPayCountRecord{})
	memberPayCountRecordMapping              = queries.MakeStructMapping(memberPayCountRecordType)
	memberPayCountRecordPrimaryKeyMapping, _ = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, memberPayCountRecordPrimaryKeyColumns)
	memberPayCountRecordInsertCacheMut       sync.RWMutex
	memberPayCountRecordInsertCache          = make(map[string]insertCache)
	memberPayCountRecordUpdateCacheMut       sync.RWMutex
	memberPayCountRecordUpdateCache          = make(map[string]updateCache)
	memberPayCountRecordUpsertCacheMut       sync.RWMutex
	memberPayCountRecordUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var memberPayCountRecordBeforeInsertHooks []MemberPayCountRecordHook
var memberPayCountRecordBeforeUpdateHooks []MemberPayCountRecordHook
var memberPayCountRecordBeforeDeleteHooks []MemberPayCountRecordHook
var memberPayCountRecordBeforeUpsertHooks []MemberPayCountRecordHook

var memberPayCountRecordAfterInsertHooks []MemberPayCountRecordHook
var memberPayCountRecordAfterSelectHooks []MemberPayCountRecordHook
var memberPayCountRecordAfterUpdateHooks []MemberPayCountRecordHook
var memberPayCountRecordAfterDeleteHooks []MemberPayCountRecordHook
var memberPayCountRecordAfterUpsertHooks []MemberPayCountRecordHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MemberPayCountRecord) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MemberPayCountRecord) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MemberPayCountRecord) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MemberPayCountRecord) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MemberPayCountRecord) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MemberPayCountRecord) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MemberPayCountRecord) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MemberPayCountRecord) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MemberPayCountRecord) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range memberPayCountRecordAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMemberPayCountRecordHook registers your hook function for all future operations.
func AddMemberPayCountRecordHook(hookPoint boil.HookPoint, memberPayCountRecordHook MemberPayCountRecordHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		memberPayCountRecordBeforeInsertHooks = append(memberPayCountRecordBeforeInsertHooks, memberPayCountRecordHook)
	case boil.BeforeUpdateHook:
		memberPayCountRecordBeforeUpdateHooks = append(memberPayCountRecordBeforeUpdateHooks, memberPayCountRecordHook)
	case boil.BeforeDeleteHook:
		memberPayCountRecordBeforeDeleteHooks = append(memberPayCountRecordBeforeDeleteHooks, memberPayCountRecordHook)
	case boil.BeforeUpsertHook:
		memberPayCountRecordBeforeUpsertHooks = append(memberPayCountRecordBeforeUpsertHooks, memberPayCountRecordHook)
	case boil.AfterInsertHook:
		memberPayCountRecordAfterInsertHooks = append(memberPayCountRecordAfterInsertHooks, memberPayCountRecordHook)
	case boil.AfterSelectHook:
		memberPayCountRecordAfterSelectHooks = append(memberPayCountRecordAfterSelectHooks, memberPayCountRecordHook)
	case boil.AfterUpdateHook:
		memberPayCountRecordAfterUpdateHooks = append(memberPayCountRecordAfterUpdateHooks, memberPayCountRecordHook)
	case boil.AfterDeleteHook:
		memberPayCountRecordAfterDeleteHooks = append(memberPayCountRecordAfterDeleteHooks, memberPayCountRecordHook)
	case boil.AfterUpsertHook:
		memberPayCountRecordAfterUpsertHooks = append(memberPayCountRecordAfterUpsertHooks, memberPayCountRecordHook)
	}
}

// One returns a single memberPayCountRecord record from the query.
func (q memberPayCountRecordQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MemberPayCountRecord, error) {
	o := &MemberPayCountRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for member_pay_count_record")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MemberPayCountRecord records from the query.
func (q memberPayCountRecordQuery) All(ctx context.Context, exec boil.ContextExecutor) (MemberPayCountRecordSlice, error) {
	var o []*MemberPayCountRecord

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MemberPayCountRecord slice")
	}

	if len(memberPayCountRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MemberPayCountRecord records in the query.
func (q memberPayCountRecordQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count member_pay_count_record rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q memberPayCountRecordQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if member_pay_count_record exists")
	}

	return count > 0, nil
}

// MemberPayCountRecords retrieves all the records using an executor.
func MemberPayCountRecords(mods ...qm.QueryMod) memberPayCountRecordQuery {
	mods = append(mods, qm.From("`member_pay_count_record`"))
	return memberPayCountRecordQuery{NewQuery(mods...)}
}

// FindMemberPayCountRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMemberPayCountRecord(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MemberPayCountRecord, error) {
	memberPayCountRecordObj := &MemberPayCountRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `member_pay_count_record` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, memberPayCountRecordObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from member_pay_count_record")
	}

	if err = memberPayCountRecordObj.doAfterSelectHooks(ctx, exec); err != nil {
		return memberPayCountRecordObj, err
	}

	return memberPayCountRecordObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MemberPayCountRecord) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_pay_count_record provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberPayCountRecordColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	memberPayCountRecordInsertCacheMut.RLock()
	cache, cached := memberPayCountRecordInsertCache[key]
	memberPayCountRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			memberPayCountRecordAllColumns,
			memberPayCountRecordColumnsWithDefault,
			memberPayCountRecordColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `member_pay_count_record` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `member_pay_count_record` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `member_pay_count_record` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, memberPayCountRecordPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into member_pay_count_record")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberPayCountRecordMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_pay_count_record")
	}

CacheNoHooks:
	if !cached {
		memberPayCountRecordInsertCacheMut.Lock()
		memberPayCountRecordInsertCache[key] = cache
		memberPayCountRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MemberPayCountRecord.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MemberPayCountRecord) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	memberPayCountRecordUpdateCacheMut.RLock()
	cache, cached := memberPayCountRecordUpdateCache[key]
	memberPayCountRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			memberPayCountRecordAllColumns,
			memberPayCountRecordPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update member_pay_count_record, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `member_pay_count_record` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, memberPayCountRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, append(wl, memberPayCountRecordPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update member_pay_count_record row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for member_pay_count_record")
	}

	if !cached {
		memberPayCountRecordUpdateCacheMut.Lock()
		memberPayCountRecordUpdateCache[key] = cache
		memberPayCountRecordUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q memberPayCountRecordQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for member_pay_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for member_pay_count_record")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MemberPayCountRecordSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberPayCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `member_pay_count_record` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberPayCountRecordPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in memberPayCountRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all memberPayCountRecord")
	}
	return rowsAff, nil
}

var mySQLMemberPayCountRecordUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MemberPayCountRecord) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no member_pay_count_record provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(memberPayCountRecordColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLMemberPayCountRecordUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	memberPayCountRecordUpsertCacheMut.RLock()
	cache, cached := memberPayCountRecordUpsertCache[key]
	memberPayCountRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			memberPayCountRecordAllColumns,
			memberPayCountRecordColumnsWithDefault,
			memberPayCountRecordColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			memberPayCountRecordAllColumns,
			memberPayCountRecordPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert member_pay_count_record, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`member_pay_count_record`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `member_pay_count_record` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for member_pay_count_record")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == memberPayCountRecordMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(memberPayCountRecordType, memberPayCountRecordMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for member_pay_count_record")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for member_pay_count_record")
	}

CacheNoHooks:
	if !cached {
		memberPayCountRecordUpsertCacheMut.Lock()
		memberPayCountRecordUpsertCache[key] = cache
		memberPayCountRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MemberPayCountRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MemberPayCountRecord) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MemberPayCountRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), memberPayCountRecordPrimaryKeyMapping)
	sql := "DELETE FROM `member_pay_count_record` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from member_pay_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for member_pay_count_record")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q memberPayCountRecordQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no memberPayCountRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from member_pay_count_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_pay_count_record")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MemberPayCountRecordSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(memberPayCountRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberPayCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `member_pay_count_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberPayCountRecordPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from memberPayCountRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for member_pay_count_record")
	}

	if len(memberPayCountRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MemberPayCountRecord) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMemberPayCountRecord(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MemberPayCountRecordSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MemberPayCountRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), memberPayCountRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `member_pay_count_record`.* FROM `member_pay_count_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, memberPayCountRecordPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MemberPayCountRecordSlice")
	}

	*o = slice

	return nil
}

// MemberPayCountRecordExists checks if the MemberPayCountRecord row exists.
func MemberPayCountRecordExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `member_pay_count_record` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if member_pay_count_record exists")
	}

	return exists, nil
}
