// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AdminNeedDumpIpa is an object representing the database table.
type AdminNeedDumpIpa struct {
	ID         int64     `boil:"id" json:"id,string" toml:"id" yaml:"id"`
	MemberID   int64     `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	IpaID      int64     `boil:"ipa_id" json:"ipa_id" toml:"ipa_id" yaml:"ipa_id"`
	IpaVersion string    `boil:"ipa_version" json:"ipa_version" toml:"ipa_version" yaml:"ipa_version"`
	IpaName    string    `boil:"ipa_name" json:"ipa_name" toml:"ipa_name" yaml:"ipa_name"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *adminNeedDumpIpaR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L adminNeedDumpIpaL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AdminNeedDumpIpaColumns = struct {
	ID         string
	MemberID   string
	IpaID      string
	IpaVersion string
	IpaName    string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	MemberID:   "member_id",
	IpaID:      "ipa_id",
	IpaVersion: "ipa_version",
	IpaName:    "ipa_name",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

// Generated where

var AdminNeedDumpIpaWhere = struct {
	ID         whereHelperint64
	MemberID   whereHelperint64
	IpaID      whereHelperint64
	IpaVersion whereHelperstring
	IpaName    whereHelperstring
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
}{
	ID:         whereHelperint64{field: "`admin_need_dump_ipa`.`id`"},
	MemberID:   whereHelperint64{field: "`admin_need_dump_ipa`.`member_id`"},
	IpaID:      whereHelperint64{field: "`admin_need_dump_ipa`.`ipa_id`"},
	IpaVersion: whereHelperstring{field: "`admin_need_dump_ipa`.`ipa_version`"},
	IpaName:    whereHelperstring{field: "`admin_need_dump_ipa`.`ipa_name`"},
	CreatedAt:  whereHelpertime_Time{field: "`admin_need_dump_ipa`.`created_at`"},
	UpdatedAt:  whereHelpertime_Time{field: "`admin_need_dump_ipa`.`updated_at`"},
}

// AdminNeedDumpIpaRels is where relationship names are stored.
var AdminNeedDumpIpaRels = struct {
}{}

// adminNeedDumpIpaR is where relationships are stored.
type adminNeedDumpIpaR struct {
}

// NewStruct creates a new relationship struct
func (*adminNeedDumpIpaR) NewStruct() *adminNeedDumpIpaR {
	return &adminNeedDumpIpaR{}
}

// adminNeedDumpIpaL is where Load methods for each relationship are stored.
type adminNeedDumpIpaL struct{}

var (
	adminNeedDumpIpaAllColumns            = []string{"id", "member_id", "ipa_id", "ipa_version", "ipa_name", "created_at", "updated_at"}
	adminNeedDumpIpaColumnsWithoutDefault = []string{"member_id", "ipa_id", "ipa_version", "ipa_name"}
	adminNeedDumpIpaColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	adminNeedDumpIpaPrimaryKeyColumns     = []string{"id"}
)

type (
	// AdminNeedDumpIpaSlice is an alias for a slice of pointers to AdminNeedDumpIpa.
	// This should almost always be used instead of []AdminNeedDumpIpa.
	AdminNeedDumpIpaSlice []*AdminNeedDumpIpa
	// AdminNeedDumpIpaHook is the signature for custom AdminNeedDumpIpa hook methods
	AdminNeedDumpIpaHook func(context.Context, boil.ContextExecutor, *AdminNeedDumpIpa) error

	adminNeedDumpIpaQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	adminNeedDumpIpaType                 = reflect.TypeOf(&AdminNeedDumpIpa{})
	adminNeedDumpIpaMapping              = queries.MakeStructMapping(adminNeedDumpIpaType)
	adminNeedDumpIpaPrimaryKeyMapping, _ = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, adminNeedDumpIpaPrimaryKeyColumns)
	adminNeedDumpIpaInsertCacheMut       sync.RWMutex
	adminNeedDumpIpaInsertCache          = make(map[string]insertCache)
	adminNeedDumpIpaUpdateCacheMut       sync.RWMutex
	adminNeedDumpIpaUpdateCache          = make(map[string]updateCache)
	adminNeedDumpIpaUpsertCacheMut       sync.RWMutex
	adminNeedDumpIpaUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var adminNeedDumpIpaBeforeInsertHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaBeforeUpdateHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaBeforeDeleteHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaBeforeUpsertHooks []AdminNeedDumpIpaHook

var adminNeedDumpIpaAfterInsertHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaAfterSelectHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaAfterUpdateHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaAfterDeleteHooks []AdminNeedDumpIpaHook
var adminNeedDumpIpaAfterUpsertHooks []AdminNeedDumpIpaHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AdminNeedDumpIpa) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AdminNeedDumpIpa) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AdminNeedDumpIpa) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AdminNeedDumpIpa) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AdminNeedDumpIpa) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AdminNeedDumpIpa) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AdminNeedDumpIpa) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AdminNeedDumpIpa) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AdminNeedDumpIpa) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range adminNeedDumpIpaAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAdminNeedDumpIpaHook registers your hook function for all future operations.
func AddAdminNeedDumpIpaHook(hookPoint boil.HookPoint, adminNeedDumpIpaHook AdminNeedDumpIpaHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		adminNeedDumpIpaBeforeInsertHooks = append(adminNeedDumpIpaBeforeInsertHooks, adminNeedDumpIpaHook)
	case boil.BeforeUpdateHook:
		adminNeedDumpIpaBeforeUpdateHooks = append(adminNeedDumpIpaBeforeUpdateHooks, adminNeedDumpIpaHook)
	case boil.BeforeDeleteHook:
		adminNeedDumpIpaBeforeDeleteHooks = append(adminNeedDumpIpaBeforeDeleteHooks, adminNeedDumpIpaHook)
	case boil.BeforeUpsertHook:
		adminNeedDumpIpaBeforeUpsertHooks = append(adminNeedDumpIpaBeforeUpsertHooks, adminNeedDumpIpaHook)
	case boil.AfterInsertHook:
		adminNeedDumpIpaAfterInsertHooks = append(adminNeedDumpIpaAfterInsertHooks, adminNeedDumpIpaHook)
	case boil.AfterSelectHook:
		adminNeedDumpIpaAfterSelectHooks = append(adminNeedDumpIpaAfterSelectHooks, adminNeedDumpIpaHook)
	case boil.AfterUpdateHook:
		adminNeedDumpIpaAfterUpdateHooks = append(adminNeedDumpIpaAfterUpdateHooks, adminNeedDumpIpaHook)
	case boil.AfterDeleteHook:
		adminNeedDumpIpaAfterDeleteHooks = append(adminNeedDumpIpaAfterDeleteHooks, adminNeedDumpIpaHook)
	case boil.AfterUpsertHook:
		adminNeedDumpIpaAfterUpsertHooks = append(adminNeedDumpIpaAfterUpsertHooks, adminNeedDumpIpaHook)
	}
}

// One returns a single adminNeedDumpIpa record from the query.
func (q adminNeedDumpIpaQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AdminNeedDumpIpa, error) {
	o := &AdminNeedDumpIpa{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for admin_need_dump_ipa")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AdminNeedDumpIpa records from the query.
func (q adminNeedDumpIpaQuery) All(ctx context.Context, exec boil.ContextExecutor) (AdminNeedDumpIpaSlice, error) {
	var o []*AdminNeedDumpIpa

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AdminNeedDumpIpa slice")
	}

	if len(adminNeedDumpIpaAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AdminNeedDumpIpa records in the query.
func (q adminNeedDumpIpaQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count admin_need_dump_ipa rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q adminNeedDumpIpaQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if admin_need_dump_ipa exists")
	}

	return count > 0, nil
}

// AdminNeedDumpIpas retrieves all the records using an executor.
func AdminNeedDumpIpas(mods ...qm.QueryMod) adminNeedDumpIpaQuery {
	mods = append(mods, qm.From("`admin_need_dump_ipa`"))
	return adminNeedDumpIpaQuery{NewQuery(mods...)}
}

// FindAdminNeedDumpIpa retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAdminNeedDumpIpa(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*AdminNeedDumpIpa, error) {
	adminNeedDumpIpaObj := &AdminNeedDumpIpa{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `admin_need_dump_ipa` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, adminNeedDumpIpaObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from admin_need_dump_ipa")
	}

	if err = adminNeedDumpIpaObj.doAfterSelectHooks(ctx, exec); err != nil {
		return adminNeedDumpIpaObj, err
	}

	return adminNeedDumpIpaObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AdminNeedDumpIpa) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no admin_need_dump_ipa provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(adminNeedDumpIpaColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	adminNeedDumpIpaInsertCacheMut.RLock()
	cache, cached := adminNeedDumpIpaInsertCache[key]
	adminNeedDumpIpaInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			adminNeedDumpIpaAllColumns,
			adminNeedDumpIpaColumnsWithDefault,
			adminNeedDumpIpaColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `admin_need_dump_ipa` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `admin_need_dump_ipa` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `admin_need_dump_ipa` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, adminNeedDumpIpaPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into admin_need_dump_ipa")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == adminNeedDumpIpaMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for admin_need_dump_ipa")
	}

CacheNoHooks:
	if !cached {
		adminNeedDumpIpaInsertCacheMut.Lock()
		adminNeedDumpIpaInsertCache[key] = cache
		adminNeedDumpIpaInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AdminNeedDumpIpa.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AdminNeedDumpIpa) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	adminNeedDumpIpaUpdateCacheMut.RLock()
	cache, cached := adminNeedDumpIpaUpdateCache[key]
	adminNeedDumpIpaUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			adminNeedDumpIpaAllColumns,
			adminNeedDumpIpaPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update admin_need_dump_ipa, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `admin_need_dump_ipa` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, adminNeedDumpIpaPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, append(wl, adminNeedDumpIpaPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update admin_need_dump_ipa row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for admin_need_dump_ipa")
	}

	if !cached {
		adminNeedDumpIpaUpdateCacheMut.Lock()
		adminNeedDumpIpaUpdateCache[key] = cache
		adminNeedDumpIpaUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q adminNeedDumpIpaQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for admin_need_dump_ipa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for admin_need_dump_ipa")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AdminNeedDumpIpaSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminNeedDumpIpaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `admin_need_dump_ipa` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminNeedDumpIpaPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in adminNeedDumpIpa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all adminNeedDumpIpa")
	}
	return rowsAff, nil
}

var mySQLAdminNeedDumpIpaUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AdminNeedDumpIpa) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no admin_need_dump_ipa provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(adminNeedDumpIpaColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAdminNeedDumpIpaUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	adminNeedDumpIpaUpsertCacheMut.RLock()
	cache, cached := adminNeedDumpIpaUpsertCache[key]
	adminNeedDumpIpaUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			adminNeedDumpIpaAllColumns,
			adminNeedDumpIpaColumnsWithDefault,
			adminNeedDumpIpaColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			adminNeedDumpIpaAllColumns,
			adminNeedDumpIpaPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert admin_need_dump_ipa, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`admin_need_dump_ipa`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `admin_need_dump_ipa` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for admin_need_dump_ipa")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == adminNeedDumpIpaMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(adminNeedDumpIpaType, adminNeedDumpIpaMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for admin_need_dump_ipa")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for admin_need_dump_ipa")
	}

CacheNoHooks:
	if !cached {
		adminNeedDumpIpaUpsertCacheMut.Lock()
		adminNeedDumpIpaUpsertCache[key] = cache
		adminNeedDumpIpaUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AdminNeedDumpIpa record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AdminNeedDumpIpa) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AdminNeedDumpIpa provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), adminNeedDumpIpaPrimaryKeyMapping)
	sql := "DELETE FROM `admin_need_dump_ipa` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from admin_need_dump_ipa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for admin_need_dump_ipa")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q adminNeedDumpIpaQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no adminNeedDumpIpaQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from admin_need_dump_ipa")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for admin_need_dump_ipa")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AdminNeedDumpIpaSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(adminNeedDumpIpaBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminNeedDumpIpaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `admin_need_dump_ipa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminNeedDumpIpaPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from adminNeedDumpIpa slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for admin_need_dump_ipa")
	}

	if len(adminNeedDumpIpaAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AdminNeedDumpIpa) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAdminNeedDumpIpa(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AdminNeedDumpIpaSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AdminNeedDumpIpaSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), adminNeedDumpIpaPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `admin_need_dump_ipa`.* FROM `admin_need_dump_ipa` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, adminNeedDumpIpaPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AdminNeedDumpIpaSlice")
	}

	*o = slice

	return nil
}

// AdminNeedDumpIpaExists checks if the AdminNeedDumpIpa row exists.
func AdminNeedDumpIpaExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `admin_need_dump_ipa` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if admin_need_dump_ipa exists")
	}

	return exists, nil
}
